name: Agent Builder (Context Aware)

on:
  issue_comment:
    types: [created]

jobs:
  builder:
    if: contains(github.event.comment.body, '/code-zhangxiaohao-agent')
    runs-on: ubuntu-latest
    permissions:
      contents: write
      pull-requests: write
      issues: write

    steps:
      - name: Checkout Code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Generate File Tree
        id: file_tree
        run: |
          TREE=$(find . -maxdepth 5 -not -path '*/.*' -not -path '*/node_modules*' -not -path '*/dist*' -not -path '*/build*' -not -path '*/.next*' | sed 's|./||' | sort)
          echo "TREE<<EOF" >> $GITHUB_ENV
          echo "$TREE" >> $GITHUB_ENV
          echo "EOF" >> $GITHUB_ENV

      - name: Execute Frontend Implementation
        uses: actions/github-script@v7
        env:
          OPENROUTER_API_KEY: ${{ secrets.OPENROUTER_API_KEY }}
          ISSUE_BODY: ${{ github.event.issue.body }}
          ISSUE_TITLE: ${{ github.event.issue.title }}
          PROJECT_TREE: ${{ env.TREE }}
        with:
          script: |
            const projectTree = process.env.PROJECT_TREE;
            const existingFiles = new Set(projectTree.split('\n').filter(f => f.trim()));
            
            // 项目结构规范
            const PROJECT_STRUCTURE = {
              pages: 'frontend/app',
              components: 'frontend/components',
              uiComponents: 'frontend/components/ui',
              featureComponents: 'frontend/components/features',
              hooks: 'frontend/hooks',
              lib: 'frontend/lib',
              types: 'frontend/types',
              validExtensions: ['.tsx', '.ts', '.css', '.json'],
              protectedFiles: ['frontend/app/layout.tsx', 'frontend/app/globals.css', 'frontend/package.json']
            };

            // 检查文件是否存在（支持指定分支）
            const fileExists = async (path, branch = null) => {
              try {
                const params = { ...context.repo, path };
                if (branch) {
                  params.ref = branch;
                }
                await github.rest.repos.getContent(params);
                return true;
              } catch (e) {
                return false;
              }
            };

            // 读取文件内容（支持指定分支）
            const readFileContent = async (path, branch = null) => {
              try {
                const params = { ...context.repo, path };
                if (branch) {
                  params.ref = branch;
                }
                const { data } = await github.rest.repos.getContent(params);
                if (data.size < 80000 && data.content) {
                  return Buffer.from(data.content, 'base64').toString();
                }
              } catch (e) {}
              return null;
            };

            // 验证路径是否符合项目规范（支持父级规范覆盖）
            const validatePath = (path, parentSpecText = '') => {
              const errors = [];
              
              // 检查路径是否在父级规范中被明确提及（如果是，则允许）
              const isInParentSpec = parentSpecText && (
                parentSpecText.includes(path) || 
                parentSpecText.includes(path.replace('frontend/', ''))
              );
              
              // 页面文件必须在 app/ 目录
              if (path.includes('/page.tsx') && !path.startsWith('frontend/app/')) {
                if (!isInParentSpec) {
                  errors.push(`页面文件必须在 frontend/app/ 目录: ${path}`);
                }
              }
              
              // 组件不能直接放在 app/ 目录（除了 page.tsx, layout.tsx 等）
              const appOnlyFiles = ['page.tsx', 'layout.tsx', 'loading.tsx', 'error.tsx', 'not-found.tsx', 'template.tsx'];
              if (path.startsWith('frontend/app/') && path.endsWith('.tsx')) {
                const fileName = path.split('/').pop();
                if (!appOnlyFiles.includes(fileName) && !isInParentSpec) {
                  errors.push(`组件不应放在 app/ 目录，应放在 components/: ${path}`);
                }
              }
              
              // UI 组件应放在 components/ui/
              if (path.includes('Button') || path.includes('Input') || path.includes('Modal') || path.includes('Dialog')) {
                if (!path.includes('components/ui/') && !path.includes('components/features/') && !isInParentSpec) {
                  errors.push(`UI 组件应放在 components/ui/ 或 components/features/: ${path}`);
                }
              }
              
              return errors;
            };

            // 1. 读取当前 issue 的评论
            console.log("📥 读取当前 issue 的评论...");
            const comments = await github.rest.issues.listComments({
              ...context.repo,
              issue_number: context.issue.number
            });
            
            // 识别前端规范和前端方案的标记（放宽匹配，包含"规范"关键词即可）
            const frontendSpecMarkers = [
              /规范/i,
              /前端规范/i,
              /前端约束/i,
              /frontend spec/i,
              /frontend constraint/i,
              /\[前端规范\]/i,
              /\[前端约束\]/i,
              /## 前端规范/i,
              /## 前端约束/i
            ];
            
            const frontendPlanMarkers = [
              /前端实施方案/i,
              /前端实施计划/i,
              /方案/i,
              /frontend implementation plan/i,
              /frontend implementation/i
            ];
            
            // 识别分支策略的标记
            // "基于"表示从指定分支读取文件，但写入到当前分支
            // "使用"、"在"表示直接在指定分支上工作
            const branchMarkers = [
              { pattern: /基于分支[\s:：]+([^\s\n]+)/i, type: 'base' }, // 基于分支xxx
              { pattern: /基于[\s:：]+([^\s\n]+)\s*分支/i, type: 'base' }, // 基于某某分支
              { pattern: /基于[\s:：]+([^\s\n]+)/i, type: 'base' }, // 基于某某
              { pattern: /使用分支[\s:：]+([^\s\n]+)/i, type: 'work' }, // 使用分支
              { pattern: /在分支[\s:：]+([^\s\n]+)/i, type: 'work' }, // 在分支
              { pattern: /使用[\s:：]+([^\s\n]+)\s*分支/i, type: 'work' }, // 使用某某分支
              { pattern: /分支[\s:：]+([^\s\n]+)/i, type: 'base' }, // 分支xxx（默认作为"基于"处理）
              { pattern: /branch[\s:：]+([^\s\n]+)/i, type: 'base' }, // branch xxx（默认作为"基于"处理）
              { pattern: /zhangxiaohao\/issue-(\d+)/i, type: 'base' } // zhangxiaohao/issue-xxx（默认作为"基于"处理）
            ];
            
            // 从当前 issue 的评论中提取前端规范、前端方案和分支信息
            let frontendSpec = '';
            let frontendPlan = '';
            let branchFromComment = null; // 从评论中提取的工作分支（写入目标）
            let baseBranchFromComment = null; // 从评论中提取的基础分支（读取源）
            const referencedContent = []; // 存储引用的内容
            
            // 找到触发评论（包含 /code-zhangxiaohao-agent 的评论）
            const triggerComment = comments.data.find(c => c.body.includes('/code-zhangxiaohao-agent'));
            
            // 直接从触发评论中提取所有链接并读取内容
            if (triggerComment) {
              // 匹配所有 GitHub URL
              const urlPattern = /https:\/\/github\.com\/[^\s\)]+/g;
              const urls = triggerComment.body.match(urlPattern) || [];
              
              for (const url of urls) {
                try {
                  console.log('📎 读取链接: ' + url);
                  
                  // 尝试解析 URL 类型并读取内容
                  let content = '';
                  let source = url;
                  
                  // 如果是评论链接
                  const commentMatch = url.match(/github\.com\/([^\/]+)\/([^\/]+)\/issues\/\d+#issuecomment-(\d+)/);
                  if (commentMatch) {
                    const comment = await github.rest.issues.getComment({
                      owner: commentMatch[1],
                      repo: commentMatch[2],
                      comment_id: parseInt(commentMatch[3])
                    });
                    content = comment.data.body;
                    source = commentMatch[1] + '/' + commentMatch[2] + ' Comment';
                  }
                  // 如果是 Issue 链接
                  else if (url.includes('/issues/')) {
                    const issueMatch = url.match(/github\.com\/([^\/]+)\/([^\/]+)\/issues\/(\d+)/);
                    if (issueMatch) {
                      const issue = await github.rest.issues.get({
                        owner: issueMatch[1],
                        repo: issueMatch[2],
                        issue_number: parseInt(issueMatch[3])
                      });
                      content = issue.data.body || '';
                      source = issueMatch[1] + '/' + issueMatch[2] + ' Issue #' + issueMatch[3];
                    }
                  }
                  
                  if (content) {
                    referencedContent.push({
                      source: source,
                      url: url,
                      content: content
                    });
                    console.log('✅ 已读取: ' + url);
                  }
                } catch(e) {
                  console.log('⚠️ 读取失败: ' + url + ' - ' + e.message);
                }
              }
            }
            
            // 只在触发评论中提取规范和方案信息
            if (triggerComment) {
              const hasSpec = frontendSpecMarkers.some(marker => marker.test(triggerComment.body));
              const hasPlan = frontendPlanMarkers.some(marker => marker.test(triggerComment.body));
              
              // 检查是否包含分支信息
              let branchMatch = null;
              let branchMatchType = null;
              for (const marker of branchMarkers) {
                const match = triggerComment.body.match(marker.pattern);
                if (match) {
                  branchMatch = match;
                  branchMatchType = marker.type;
                  break;
                }
              }
              
              if (hasSpec) {
                frontendSpec = triggerComment.body;
                console.log(`📋 发现前端规范: ${triggerComment.html_url}`);
              }
              if (hasPlan) {
                frontendPlan = triggerComment.body;
                console.log(`📋 发现前端方案: ${triggerComment.html_url}`);
              }
              if (branchMatch) {
                let branchValue = branchMatch[1].trim();
                
                // 去掉远程仓库前缀（如 origin/, remotes/origin/ 等）
                branchValue = branchValue.replace(/^(origin|remotes\/origin|remotes)\//, '');
                
                let normalizedBranch = '';
                if (/^\d+$/.test(branchValue)) {
                  normalizedBranch = `zhangxiaohao/issue-${branchValue}`;
                } else if (branchValue.startsWith('zhangxiaohao/issue-')) {
                  normalizedBranch = branchValue;
                } else {
                  normalizedBranch = branchValue;
                }
                
                if (branchMatchType === 'base') {
                  // "基于"分支：从指定分支读取文件，但写入到当前分支
                  baseBranchFromComment = normalizedBranch;
                  console.log(`🔗 从评论中发现基础分支（读取源）: ${baseBranchFromComment} (${triggerComment.html_url})`);
                } else {
                  // "使用"分支：直接在指定分支上工作
                  branchFromComment = normalizedBranch;
                  console.log(`🔗 从评论中发现工作分支（写入目标）: ${branchFromComment} (${triggerComment.html_url})`);
                }
              }
            }
            
            // 收集触发评论内容（用于 ISSUE_COMMENTS 变量）
            const triggerCommentContent = triggerComment ? triggerComment.body : '';
            
            // 收集其他评论作为讨论历史（排除触发评论，用于向后兼容）
            const generalComments = comments.data
              .filter(c => !c.body.includes('/code-zhangxiaohao-agent'))
              .sort((a, b) => new Date(a.created_at) - new Date(b.created_at));
            
            const discussionHistory = generalComments
              .map(c => `[User: ${c.user.login}]:\n${c.body}`)
              .join("\n\n---\n\n");
            
            // 合并所有文本来源（用于文件路径识别）
            const fullDiscussionText = comments.data.map(c => c.body).join('\n') + '\n' + process.env.ISSUE_BODY;
            let contextBody = process.env.ISSUE_BODY;
            
            // 添加触发评论的完整内容（让 LLM 自己分析）
            if (triggerComment) {
              contextBody += '\n\n--- [触发评论] ---\n';
              contextBody += '以下是触发命令的评论内容，请根据内容判断其用途（可能包含需求、规范、方案或其他信息）：\n\n';
              contextBody += triggerComment.body + '\n';
            }
            
            if (frontendSpec) {
              contextBody += '\n\n--- [前端规范] ---\n' + frontendSpec;
            }
            if (frontendPlan) {
              contextBody += '\n\n--- [前端方案] ---\n' + frontendPlan;
            }
            
            // 添加引用的内容到上下文（让 LLM 自己判断用途）
            if (referencedContent.length > 0) {
              contextBody += '\n\n--- [引用的内容] ---\n';
              contextBody += '以下是触发评论中引用的其他评论内容，请根据内容判断其用途（可能是规范、方案或其他信息）：\n\n';
              referencedContent.forEach((ref, i) => {
                contextBody += '### 引用 ' + (i + 1) + ': ' + ref.source + '\n';
                contextBody += '来源: ' + ref.url + '\n';
                contextBody += '内容:\n' + ref.content + '\n\n';
              });
            }

            // 2. 读取父级 issue 基本信息，如果当前 issue 没有前端规范和前端方案，则从父级 issue 的评论中提取
            let parentSpec = '';
            const parentRegex = /(?:Parent|Ref|父任务|父级|父需求|关联父需求)[\s:：]+#(\d+)/i;
            const parentMatch = contextBody.match(parentRegex) || process.env.ISSUE_BODY.match(parentRegex);
            
            if (parentMatch) {
              try {
                const parentId = parentMatch[1];
                console.log(`🔗 读取父级 issue: #${parentId}`);
                
                // 读取父级 issue 的基本信息（body）
                const p = await github.rest.issues.get({ ...context.repo, issue_number: parentId });
                parentSpec = p.data.body || '';
                
                // 如果当前 issue 的评论中没有前端规范和前端方案，则从父级 issue 的评论中提取
                if (!frontendSpec && !frontendPlan) {
                  console.log(`📋 当前 issue 没有前端规范和前端方案，尝试从父级 issue 评论中提取...`);
                  
                  // 读取父级 issue 的评论
                  const parentComments = await github.rest.issues.listComments({
                    ...context.repo,
                    issue_number: parentId
                  });
                  
                  // 按时间排序（最早的在前）
                  const sortedParentComments = parentComments.data.sort((a, b) => 
                    new Date(a.created_at) - new Date(b.created_at)
                  );
                  
                  sortedParentComments.forEach(comment => {
                    const hasSpec = frontendSpecMarkers.some(marker => marker.test(comment.body));
                    const hasPlan = frontendPlanMarkers.some(marker => marker.test(comment.body));
                    
                    if (hasSpec && !frontendSpec) {
                      // 提取父级前端规范（后面的会覆盖前面的）
                      frontendSpec = comment.body;
                      console.log(`📋 从父级 issue 发现前端规范: ${comment.html_url}`);
                    } else if (hasPlan && !frontendPlan) {
                      // 提取父级前端方案（后面的会覆盖前面的）
                      frontendPlan = comment.body;
                      console.log(`📋 从父级 issue 发现前端方案: ${comment.html_url}`);
                    }
                  });
                  
                  // 更新 contextBody（如果从父级提取到了规范和方案）
                  if (frontendSpec) {
                    contextBody += '\n\n--- [前端规范（来自父级 Issue）] ---\n' + frontendSpec;
                  }
                  if (frontendPlan) {
                    contextBody += '\n\n--- [前端方案（来自父级 Issue）] ---\n' + frontendPlan;
                  }
                }
                
                if (parentSpec) {
                  contextBody += '\n\n--- [父级 Issue 基本信息] ---\n' + parentSpec;
                  console.log(`✅ 已读取父级 issue 基本信息，长度: ${parentSpec.length} 字符`);
                }
              } catch(e) {
                console.log(`⚠️ 读取父级 issue 失败: ${e.message}`);
              }
            }

            // 5.5. 确定分支名
            // 读取分支：如果指定了基础分支，从基础分支读取；否则从工作分支读取
            // 写入分支：如果指定了工作分支，写入到工作分支；否则写入到当前 issue 分支
            let readBranch = null; // 读取文件的分支
            let writeBranch = `zhangxiaohao/issue-${context.issue.number}`; // 写入文件的分支
            
            // 确定写入分支
            if (branchFromComment) {
              writeBranch = branchFromComment;
              console.log(`🔗 工作分支（写入目标）: ${writeBranch}`);
            } else {
              console.log(`🔗 工作分支（写入目标）: ${writeBranch} (当前issue分支)`);
            }
            
            // 确定读取分支
            if (baseBranchFromComment) {
              // 如果指定了基础分支，从基础分支读取文件
              readBranch = baseBranchFromComment;
              console.log(`📖 基础分支（读取源）: ${readBranch}`);
            } else {
              // 否则从工作分支读取文件
              readBranch = writeBranch;
              console.log(`📖 读取分支: ${readBranch} (与工作分支相同)`);
            }
            
            // 检查文件在当前分支是否存在
            const fileExistsInBranch = async (path, branch) => {
              try {
                await github.rest.repos.getContent({ 
                  ...context.repo, 
                  path,
                  ref: branch
                });
                return true;
              } catch (e) {
                return false;
              }
            };
            
            // 3. 文件路径识别和读取
            console.log("🔍 扫描相关文件...");
            
            const existingFilesContext = [];
            const newFilePaths = [];
            let foundPaths = new Set();
            
            // 统一逻辑：先从文本中识别文件路径，如果没有找到，则读取当前分支中的前端文件
            const allTextSources = fullDiscussionText + '\n' + (parentSpec || '');
            
            // 多种路径格式匹配（支持 app/(main) 等路由组）
            const pathPatterns = [
              /(?:frontend|src)\/[\w\-\.\/\(\)]+\.(?:tsx|ts|css|json)/g,
              /app\/[\w\-\.\/\(\)]+\.(?:tsx|ts)/g,
              /components\/[\w\-\.\/]+\.(?:tsx|ts)/g,
              /hooks\/[\w\-\.\/]+\.(?:tsx|ts)/g,
              /lib\/[\w\-\.\/]+\.(?:tsx|ts)/g,
              /types\/[\w\-\.\/]+\.(?:tsx|ts)/g
            ];
            
            for (const pattern of pathPatterns) {
              const matches = allTextSources.match(pattern) || [];
              matches.forEach(p => {
                let normalized = p;
                if (!normalized.startsWith('frontend/') && !normalized.startsWith('src/')) {
                  normalized = 'frontend/' + normalized;
                }
                foundPaths.add(normalized);
              });
            }
            
            // 如果从文本中没有找到文件路径，读取当前分支中的前端文件，让大模型自己判断
            if (foundPaths.size === 0) {
              console.log('📋 文本中没有找到文件路径，读取当前分支中的前端文件，让大模型判断需要修改哪些文件');
              
              try {
                const branchRef = await github.rest.git.getRef({
                  ...context.repo,
                  ref: `heads/${readBranch}`
                });
                const commit = await github.rest.git.getCommit({
                  ...context.repo,
                  commit_sha: branchRef.data.object.sha
                });
                const tree = await github.rest.git.getTree({
                  ...context.repo,
                  tree_sha: commit.data.tree.sha,
                  recursive: true
                });
                
                // 筛选前端文件（.tsx, .ts），最多读取 50 个文件
                const frontendFiles = tree.data.tree
                  .filter(item => item.type === 'blob' && 
                    (item.path.startsWith('frontend/') || item.path.startsWith('src/')) &&
                    (item.path.endsWith('.tsx') || item.path.endsWith('.ts')) &&
                    !item.path.includes('node_modules') &&
                    !item.path.includes('.next') &&
                    !item.path.includes('dist') &&
                    !item.path.includes('test') &&
                    !item.path.includes('spec'))
                  .slice(0, 50)
                  .map(item => item.path);
                
                console.log(`📋 找到 ${frontendFiles.length} 个前端文件，将全部提供给大模型判断`);
                
                // 将所有前端文件添加到 foundPaths
                frontendFiles.forEach(path => foundPaths.add(path));
              } catch (e) {
                console.log(`⚠️ 读取分支文件列表失败: ${e.message}`);
              }
            } else {
              console.log(`📋 从文本中识别到 ${foundPaths.size} 个文件路径`);
            }
            
            // 4. 从读取分支读取文件内容
            for (const path of foundPaths) {
              // 先检查文件在读取分支是否存在
              const existsInReadBranch = await fileExistsInBranch(path, readBranch);
              // 如果读取分支不存在，检查 main 分支是否存在（用于新建文件的情况）
              const existsInMain = existsInReadBranch ? false : await fileExists(path);
              
              if (existsInReadBranch || existsInMain) {
                // 优先从读取分支读取，如果读取分支不存在则从 main 分支读取
                const content = await readFileContent(path, existsInReadBranch ? readBranch : null);
                if (content) {
                  // 如果文件在写入分支也存在，标记为 MODIFY；否则标记为 REFERENCE
                  const existsInWriteBranch = await fileExistsInBranch(path, writeBranch);
                  existingFilesContext.push({ path, content, action: existsInWriteBranch ? 'MODIFY' : 'REFERENCE' });
                  console.log(`📄 [存在${existsInReadBranch ? '于读取分支' : '于main分支'}] ${path}`);
                }
              } else {
                newFilePaths.push(path);
                console.log(`🆕 [新建] ${path}`);
              }
            }

            // 读取关键上下文文件（从读取分支）
            const criticalFiles = ['frontend/app/layout.tsx', 'frontend/app/globals.css'];
            for (const path of criticalFiles) {
              if (!existingFilesContext.find(f => f.path === path)) {
                const content = await readFileContent(path, readBranch);
                if (!content) {
                  // 如果读取分支没有，尝试从 main 分支读取
                  const mainContent = await readFileContent(path);
                  if (mainContent) {
                    existingFilesContext.push({ path, content: mainContent, action: 'REFERENCE' });
                  }
                } else {
                  existingFilesContext.push({ path, content, action: 'REFERENCE' });
                }
              }
            }

            // 读取 package.json（从读取分支）
            let packageJsonContent = "{}";
            const pkgPath = await fileExists('frontend/package.json', readBranch) ? 'frontend/package.json' : 'package.json';
            const pkgContent = await readFileContent(pkgPath, readBranch);
            if (pkgContent) {
              packageJsonContent = pkgContent;
            } else {
              // 如果读取分支没有，尝试从 main 分支读取
              const mainPkgContent = await readFileContent(pkgPath);
              if (mainPkgContent) packageJsonContent = mainPkgContent;
            }

            // 读取前端开发规范文档
            let styleGuideContent = '';
            const styleGuidePath = 'frontend/STYLE_GUIDE.md';
            const styleGuideExists = await fileExists(styleGuidePath);
            if (styleGuideExists) {
              const styleGuide = await readFileContent(styleGuidePath);
              if (styleGuide) {
                // 直接使用原始内容，不需要转义
                styleGuideContent = styleGuide;
                console.log('📚 已读取前端开发规范文档: ' + styleGuidePath);
              }
            }

            // 读取 prompt 模板文件
            let promptTemplate = '';
            const promptTemplatePath = '.github/prompts/zhangxiaohao-prompt.md';
            const promptTemplateExists = await fileExists(promptTemplatePath);
            if (promptTemplateExists) {
              const template = await readFileContent(promptTemplatePath);
              if (template) {
                promptTemplate = template;
                console.log('📝 已读取 prompt 模板: ' + promptTemplatePath);
              }
            }

            // 5. 构建结构化 Prompt
            const existingFilesStr = existingFilesContext.map(f => 
              '### ' + f.action + ': ' + f.path + '\n```\n' + f.content + '\n```'
            ).join('\n\n');

            const newFilesList = newFilePaths.length > 0 ? newFilePaths.map(p => '- ' + p).join('\n') : '无';
            
            // 5. 检查是否需要创建页面：从需求描述、前端规范和方案中提取页面路径（在确定分支后）
            let requiredPagePath = null;
            const pagePathPatterns = [
              /frontend\/app\/\(main\)\/[\w\-\.\/]+page\.tsx/g,
              /frontend\/app\/[\w\-\.\/]+page\.tsx/g,
              /app\/\(main\)\/[\w\-\.\/]+page\.tsx/g,
              /app\/[\w\-\.\/]+page\.tsx/g
            ];
            
            // 从所有文本来源中查找页面路径
            const allTextForPageCheck = (frontendSpec || '') + '\n' + (frontendPlan || '') + '\n' + contextBody + '\n' + newFilesList;
            for (const pattern of pagePathPatterns) {
              const matches = allTextForPageCheck.match(pattern);
              if (matches && matches.length > 0) {
                // 找到第一个页面路径
                let foundPath = matches[0];
                if (!foundPath.startsWith('frontend/')) {
                  foundPath = 'frontend/' + foundPath;
                }
                // 检查页面是否在写入分支已存在（而不是 main 分支）
                const pageExists = await fileExistsInBranch(foundPath, writeBranch);
                if (!pageExists) {
                  requiredPagePath = foundPath;
                  console.log(`📄 检测到需要创建的页面: ${requiredPagePath} (在写入分支 ${writeBranch} 不存在)`);
                  break;
                } else {
                  console.log(`📄 页面已存在: ${foundPath} (在写入分支 ${writeBranch})`);
                }
              }
            }
            
            // 构建页面检查提示
            let pageCheckNote = '';
            if (requiredPagePath) {
              pageCheckNote = '\n\n重要：必须创建页面\n' +
                '检测到需求中提到了页面路径 ' + requiredPagePath + '，但该页面不存在。\n' +
                '你必须创建这个页面文件，否则功能无法正常使用。\n' +
                '请确保在输出中包含：\n' +
                '### CREATE: ' + requiredPagePath + '\n' +
                '```tsx\n' +
                '(完整的页面代码)\n' +
                '```\n';
            } else if (newFilesList.includes('page.tsx') || contextBody.includes('页面') || contextBody.includes('page') || frontendSpec.includes('页面') || frontendPlan.includes('页面')) {
              // 如果提到了页面但没有找到具体路径，提醒 AI 检查
              pageCheckNote = '\n\n页面检查\n' +
                '需求中提到了页面相关的内容，请检查是否需要创建新的页面文件。\n' +
                '如果需要创建页面，请使用正确的路径格式（如 frontend/app/(main)/video/page.tsx）。\n' +
                '如果前端规范或前端方案中提到了页面路径，必须严格按照规范创建页面。\n';
            }
            
            // 构建前端规范和方案提示（优先级最高）
            let frontendSpecNote = '';
            if (frontendSpec || frontendPlan) {
              frontendSpecNote = '\n\n前端规范和方案（最高优先级 - 必须严格遵守）\n';
              frontendSpecNote += '重要：以下规范优先级高于所有其他规范（包括基础开发规范文档）。如有冲突，必须优先遵循以下规范。\n\n';
              if (frontendSpec) {
                // 检查是否来自父级 issue（通过检查 contextBody 中是否包含"来自父级 Issue"）
                const isFromParent = contextBody.includes('前端规范（来自父级 Issue）');
                const source = isFromParent ? '（来自父级 Issue 评论）' : '（来自当前 Issue 评论）';
                frontendSpecNote += '前端规范' + source + '：\n' + frontendSpec + '\n\n';
              }
              if (frontendPlan) {
                // 检查是否来自父级 issue
                const isFromParent = contextBody.includes('前端方案（来自父级 Issue）');
                const source = isFromParent ? '（来自父级 Issue 评论）' : '（来自当前 Issue 评论）';
                frontendSpecNote += '前端方案' + source + '：\n' + frontendPlan + '\n\n';
              }
            }
            
            // 构建父级规范提示
            let parentSpecNote = '';
            if (parentSpec) {
              parentSpecNote = '\n\n父级 Issue 基本信息\n' +
                '以下内容来自父级 Issue，供参考：\n' +
                parentSpec + '\n';
            }
            
            // 构建前端开发规范提示（基础规范，优先级低于评论/Issue 规范）
            let styleGuideNote = '';
            if (styleGuideContent) {
              const hasFrontendSpec = frontendSpec || frontendPlan;
              const priorityNote = hasFrontendSpec 
                ? '\n注意：这是基础开发规范。如果上面的前端规范和方案中有冲突的规范，优先遵循上面的规范。\n'
                : '\n这是项目的基础前端开发规范，所有代码必须严格遵循。\n';
              
              styleGuideNote = '\n\n前端开发规范（基础规范）\n' +
                priorityNote +
                styleGuideContent + '\n\n' +
                '基础规范要点：\n' +
                '- 必须使用语义化的 Tailwind 颜色类（如 bg-background、text-foreground），禁止硬编码颜色值\n' +
                '- 业务颜色必须使用 text-profit（盈利）或 text-loss（亏损）\n' +
                '- 组件必须从 @/components/ui 导入 shadcn/ui 组件（New York 风格）\n' +
                '- 所有函数和组件必须有完整的 TypeScript 类型定义\n' +
                '- 使用 cn() 工具函数合并类名\n' +
                '- 优先使用 Flexbox 和 Grid 布局，避免使用 position: relative 进行布局\n' +
                '- 遵循目录结构规范\n\n';
            }
            
            
            // 构建 prompt：如果存在模板文件则使用模板，否则使用默认 prompt
            let prompt = '';
            if (promptTemplate) {
              // 定义模板变量映射
              const templateVars = {
                'ISSUE_TITLE': process.env.ISSUE_TITLE || '',
                'ISSUE_BODY': process.env.ISSUE_BODY || '',
                'ISSUE_COMMENTS': triggerCommentContent || '',
                'FRONTEND_SPEC': frontendSpecNote || '',
                'PARENT_SPEC': parentSpecNote || '',
                'STYLE_GUIDE': styleGuideNote || '',
                'PACKAGE_JSON': packageJsonContent || '{}',
                'EXISTING_FILES': existingFilesStr || '无现有文件需要修改'
              };
              
              // 统一替换所有模板变量
              prompt = promptTemplate.replace(/\{\{(\w+)\}\}/g, (match, key) => {
                return templateVars[key] !== undefined ? templateVars[key] : match;
              });
              
              // 追加页面检查提示（如果存在）
              if (pageCheckNote) {
                prompt += pageCheckNote;
              }
            }

            console.log("🤖 调用 AI...");

            const aiRes = await fetch('https://openrouter.ai/api/v1/chat/completions', {
              method: 'POST',
              headers: {
                'Authorization': `Bearer ${process.env.OPENROUTER_API_KEY}`,
                'Content-Type': 'application/json'
              },
              body: JSON.stringify({
                model: "anthropic/claude-sonnet-4.5",
                messages: [{ role: "user", content: prompt }]
              })
            });
            
            if (!aiRes.ok) {
              const errText = await aiRes.text();
              await github.rest.issues.createComment({
                ...context.repo,
                issue_number: context.issue.number,
                body: 'AI API 调用失败: ' + errText.substring(0, 500)
              });
              throw new Error(`AI API Error: ${errText}`);
            }
            
            const aiData = await aiRes.json();
            const rawContent = aiData.choices[0].message.content;

            // 6. 解析 AI 输出
            console.log("📦 解析文件...");
            console.log("📄 AI 返回内容长度:", rawContent.length);
            
            const files = [];
            const lines = rawContent.split('\n');
            let currentFile = null;
            let currentAction = null;
            let currentContentLines = [];

            const cleanContent = (linesArray) => {
              let content = linesArray.join('\n').trim();
              // 移除代码块标记
              content = content.replace(/^```[\w\-\.]*\s*$/gm, '');
              content = content.replace(/^```\s*$/gm, '');
              // 移除分隔符行（--- 或类似的）
              content = content.replace(/^---+[\s-]*$/gm, '');
              // 移除 Markdown 标题（###, ##, #）
              content = content.replace(/^#{1,6}\s+.+$/gm, '');
              // 移除 Markdown 列表项（- , * , 1. 等）
              content = content.replace(/^[\s]*[-*+]\s+.+$/gm, '');
              content = content.replace(/^[\s]*\d+\.\s+.+$/gm, '');
              // 移除文件末尾的空行和分隔符
              content = content.replace(/\n+---+[\s-]*\n*$/g, '');
              content = content.replace(/\n+---+[\s-]*$/g, '');
              // 移除多余的空行（连续3个或以上空行）
              content = content.replace(/\n{3,}/g, '\n\n');
              return content.trim();
            };
            
            for (const line of lines) {
              // 检测文件标记（MODIFY 或 CREATE）
              const modifyMatch = line.trim().match(/^###\s*MODIFY:\s*(.+)$/);
              const createMatch = line.trim().match(/^###\s*CREATE:\s*(.+)$/);
              const fileMatch = line.trim().match(/^###\s*FILE:\s*(.+)$/);
              
              if (modifyMatch || createMatch || fileMatch) {
                // 保存上一个文件
                if (currentFile) {
                  files.push({ 
                    path: currentFile, 
                    content: cleanContent(currentContentLines),
                    action: currentAction 
                  });
                }
                // 开始新文件
                currentFile = (modifyMatch || createMatch || fileMatch)[1].trim();
                currentAction = modifyMatch ? 'MODIFY' : (createMatch ? 'CREATE' : 'UNKNOWN');
                currentContentLines = [];
                console.log(`发现文件: ${currentAction} ${currentFile}`);
              } else if (currentFile) {
                // 收集文件内容
                currentContentLines.push(line);
              }
            }
            
            if (currentFile) {
              files.push({ 
                path: currentFile, 
                content: cleanContent(currentContentLines),
                action: currentAction 
              });
            }

            if (files.length === 0) {
              // 输出更多调试信息
              const debugInfo = 'AI 未生成有效的文件块。\n\n' +
                '调试信息：\n' +
                '- AI 返回内容长度: ' + rawContent.length + '\n' +
                '- 是否检测到 MODIFY 标记: ' + (rawContent.includes('### MODIFY:') ? '是' : '否') + '\n' +
                '- 是否检测到 CREATE 标记: ' + (rawContent.includes('### CREATE:') ? '是' : '否') + '\n\n' +
                'AI 返回内容前2000字符：\n' +
                rawContent.substring(0, 2000) + '\n\n' +
                '请检查需求描述是否清晰，或检查 AI 是否按照 prompt 格式输出代码文件。';
              
              await github.rest.issues.createComment({
                ...context.repo,
                issue_number: context.issue.number,
                body: debugInfo
              });
              throw new Error("解析失败：AI 未生成任何有效文件块");
            }

            // 7. 验证和过滤文件
            const validFiles = [];
            const warnings = [];
            
            for (const file of files) {
              // 跳过后端文件
              if (file.path.endsWith('.go') || file.path.includes('backend/')) continue;
              
              // 验证路径（传入父级规范，允许父级规范覆盖默认规则）
              const pathErrors = validatePath(file.path, parentSpec);
              if (pathErrors.length > 0) {
                warnings.push(...pathErrors);
              }
              
              // JSON 校验
              if (file.path.endsWith('.json')) {
                try { 
                  JSON.parse(file.content); 
                } catch (e) { 
                  warnings.push(`JSON 语法错误: ${file.path}`);
                  continue; 
                }
              }
              
              // 检查 CREATE 操作的文件是否在写入分支已存在（而不是 main 分支）
              if (file.action === 'CREATE') {
                const exists = await fileExistsInBranch(file.path, writeBranch);
                if (exists) {
                  warnings.push(`文件在写入分支已存在，CREATE 操作改为 MODIFY: ${file.path}`);
                  file.action = 'MODIFY';
                }
              }
              
              // 检查受保护文件
              if (PROJECT_STRUCTURE.protectedFiles.includes(file.path) && file.action === 'CREATE') {
                warnings.push(`受保护文件不能被覆盖创建: ${file.path}`);
                file.action = 'MODIFY';
              }
              
              // 内容不能为空
              if (!file.content || file.content.trim().length === 0) {
                warnings.push(`文件内容为空，跳过: ${file.path}`);
                continue;
              }
              
              validFiles.push(file);
            }

            // 8. 创建或获取写入分支并写入文件
            // 如果指定了基础分支，从基础分支创建写入分支；否则从 main 分支创建
            let baseRef;
            if (baseBranchFromComment) {
              // 从基础分支创建写入分支
              const baseBranchRef = await github.rest.git.getRef({ ...context.repo, ref: `heads/${baseBranchFromComment}` });
              baseRef = { data: { object: { sha: baseBranchRef.data.object.sha } } };
              console.log(`📖 基于分支 ${baseBranchFromComment} 创建写入分支 ${writeBranch}`);
            } else {
              // 从 main 分支创建写入分支
              baseRef = await github.rest.git.getRef({ ...context.repo, ref: 'heads/main' });
            }
            
            // 检查写入分支是否已存在
            let branchExists = false;
            try {
              await github.rest.git.getRef({ ...context.repo, ref: `heads/${writeBranch}` });
              branchExists = true;
              console.log(`分支已存在: ${writeBranch}`);
            } catch (e) {
              // 分支不存在，创建新分支
              try {
                await github.rest.git.createRef({ 
                  ...context.repo, 
                  ref: `refs/heads/${writeBranch}`, 
                  sha: baseRef.data.object.sha 
                });
                console.log(`创建新分支: ${writeBranch}`);
              } catch (e2) {
                console.log(`创建分支失败: ${e2.message}`);
              }
            }

            const createdFiles = [];
            const modifiedFiles = [];
            
            // 批量提交：使用 Git Tree API 一次性提交所有文件
            if (validFiles.length > 0) {
              // 获取写入分支的最新 commit SHA
              let baseSha;
              try {
                const branchRef = await github.rest.git.getRef({ 
                  ...context.repo, 
                  ref: `heads/${writeBranch}` 
                });
                baseSha = branchRef.data.object.sha;
              } catch (e) {
                // 如果分支不存在，使用基础分支或 main 分支的 SHA
                baseSha = baseRef.data.object.sha;
              }
              
              // 获取基础 tree SHA
              const baseCommit = await github.rest.git.getCommit({ 
                ...context.repo, 
                commit_sha: baseSha 
              });
              const baseTreeSha = baseCommit.data.tree.sha;
              
              // 创建文件 blob 并构建 tree
              const treeItems = [];
              
              for (const file of validFiles) {
                console.log(`${file.action}: ${file.path}`);
                
                // 创建文件 blob
                const blob = await github.rest.git.createBlob({
                  ...context.repo,
                  content: Buffer.from(file.content).toString('base64'),
                  encoding: 'base64'
                });
                
                treeItems.push({
                  path: file.path,
                  mode: '100644',
                  type: 'blob',
                  sha: blob.data.sha
                });
                
                if (file.action === 'CREATE') {
                  createdFiles.push(file.path);
                } else {
                  modifiedFiles.push(file.path);
                }
              }
              
              // 创建新的 tree
              const newTree = await github.rest.git.createTree({
                ...context.repo,
                base_tree: baseTreeSha,
                tree: treeItems
              });
              
              // 创建 commit
              const commitMessage = `feat(fe): ${createdFiles.length > 0 ? 'create' : ''}${createdFiles.length > 0 && modifiedFiles.length > 0 ? ' and ' : ''}${modifiedFiles.length > 0 ? 'modify' : ''} ${validFiles.length} file${validFiles.length > 1 ? 's' : ''}`;
              
              const newCommit = await github.rest.git.createCommit({
                ...context.repo,
                message: commitMessage,
                tree: newTree.data.sha,
                parents: [baseSha]
              });
              
              // 更新写入分支引用
              await github.rest.git.updateRef({
                ...context.repo,
                ref: `heads/${writeBranch}`,
                sha: newCommit.data.sha
              });
              
              console.log(`批量提交完成: ${validFiles.length} 个文件`);
            }

            // 生成操作说明
            const generateOperationSummary = () => {
              let summary = '';
              
              // 操作类型由大模型根据 Issue 描述判断，这里统一显示
              summary += '本次操作：代码实现\n\n';
              
              // 统计操作类型
              const createCount = createdFiles.length;
              const modifyCount = modifiedFiles.length;
              
              summary += `操作统计：\n`;
              summary += `- 新建文件: ${createCount} 个\n`;
              summary += `- 修改文件: ${modifyCount} 个\n\n`;
              
              // 列出修改的文件
              if (modifyCount > 0) {
                summary += `修改的文件：\n`;
                for (const file of validFiles) {
                  if (file.action === 'MODIFY') {
                    // 尝试提取关键改动（简单分析）
                    const content = file.content;
                    const lines = content.split('\n');
                    const addedFunctions = [];
                    const modifiedPatterns = [];
                    
                    // 查找新增的函数/组件
                    for (let i = 0; i < lines.length; i++) {
                      const line = lines[i].trim();
                      if (line.match(/^(export\s+)?(function|const|class|interface|type)\s+\w+/)) {
                        const match = line.match(/(function|const|class|interface|type)\s+(\w+)/);
                        if (match && match[2]) {
                          addedFunctions.push(match[2]);
                        }
                      }
                    }
                    
                    summary += `- \`${file.path}\``;
                    if (addedFunctions.length > 0) {
                      summary += ` (涉及: ${addedFunctions.slice(0, 3).join(', ')}${addedFunctions.length > 3 ? '...' : ''})`;
                    }
                    summary += `\n`;
                  }
                }
                summary += `\n`;
              }
              
              // 列出新建的文件
              if (createCount > 0) {
                summary += `新建的文件：\n`;
                createdFiles.forEach(path => {
                  summary += `- \`${path}\`\n`;
                });
                summary += `\n`;
              }
              
              return summary;
            };

            // 9. 创建 PR
            let prBody = '###  变更清单\n\n';
            if (createdFiles.length > 0) {
              prBody += '**新建文件：**\n' + createdFiles.map(f => '- `' + f + '`').join('\n') + '\n';
            }
            if (modifiedFiles.length > 0) {
              prBody += '**修改文件：**\n' + modifiedFiles.map(f => '- `' + f + '`').join('\n') + '\n';
            }
            if (warnings.length > 0) {
              prBody += '\n### 警告\n' + warnings.map(w => '- ' + w).join('\n');
            }
            prBody += '\n\n---\n指令: `/code-zhangxiaohao-agent`\nIssue: #' + context.issue.number;

            // 生成操作说明
            const operationSummary = generateOperationSummary();
            
            try {
              const pr = await github.rest.pulls.create({
                ...context.repo,
                title: `[FE] ${process.env.ISSUE_TITLE}`,
                head: writeBranch,
                base: baseBranchFromComment || 'main',
                body: prBody
              });
              
              let commentBody = `PR: ${pr.data.html_url}\n\n`;
              if (baseBranchFromComment) {
                commentBody += `基于分支: ${baseBranchFromComment}\n`;
              }
              commentBody += operationSummary + (warnings.length > 0 ? `\n警告: ${warnings.length} 个，详见 PR\n` : '');
              
              await github.rest.issues.createComment({
                ...context.repo,
                issue_number: context.issue.number,
                body: commentBody
              });
            } catch(e) {
              let commentBody = `分支已更新: \`${writeBranch}\`\n\n`;
              if (baseBranchFromComment) {
                commentBody += `基于分支: ${baseBranchFromComment}\n`;
              }
              commentBody += operationSummary + (warnings.length > 0 ? `\n警告: ${warnings.length} 个\n` : '');
              
              await github.rest.issues.createComment({
                ...context.repo,
                issue_number: context.issue.number,
                body: commentBody
              });
            }