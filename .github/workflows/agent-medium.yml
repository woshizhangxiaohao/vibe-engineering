name: Medium Task Agent

on:
  workflow_call:
  workflow_dispatch:
    inputs:
      issue_number:
        description: 'Issue 编号'
        required: true
        type: string
      continuation_context:
        description: '继续迭代的上下文信息'
        required: false
        type: string
  issue_comment:
    types: [created]

jobs:
  medium-task:
    # 通过 workflow_call、workflow_dispatch 或 /agent-medium 命令触发
    if: |
      github.event_name == 'workflow_call' ||
      github.event_name == 'workflow_dispatch' ||
      contains(github.event.comment.body, '/agent-medium')
    runs-on: ubuntu-latest

    permissions:
      issues: write
      contents: write
      pull-requests: write
      actions: read

    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Setup Git
        run: |
          git config user.name "vibe-medium-agent"
          git config user.email "agent@vibe.bot"

      - name: Get Issue Info
        id: issue_info
        uses: actions/github-script@v7
        with:
          script: |
            let issueNumber;
            let issue;

            if (context.eventName === 'workflow_dispatch') {
              // workflow_dispatch 触发时从 inputs 获取
              issueNumber = parseInt('${{ github.event.inputs.issue_number }}');
              const { data } = await github.rest.issues.get({
                owner: context.repo.owner,
                repo: context.repo.repo,
                issue_number: issueNumber
              });
              issue = data;
            } else {
              // issue_comment 或 workflow_call 触发
              issue = context.payload.issue;
              issueNumber = issue.number;
            }

            core.setOutput('issue_number', issueNumber);
            core.setOutput('issue_title', issue.title);
            core.setOutput('issue_body', issue.body || '');

            // 存储到环境变量供后续步骤使用
            return { issueNumber, title: issue.title, body: issue.body };

      - name: Check for Duplicate Run
        id: check_duplicate
        uses: actions/github-script@v7
        with:
          script: |
            const issueNumber = ${{ steps.issue_info.outputs.issue_number }};
            const currentRunId = context.runId;

            // 检查是否有同一个 issue 的 agent 正在运行
            const { data: runs } = await github.rest.actions.listWorkflowRuns({
              owner: context.repo.owner,
              repo: context.repo.repo,
              workflow_id: 'agent-medium.yml',
              status: 'in_progress',
              per_page: 20
            });

            // 查找其他正在运行的相同 issue 任务
            for (const run of runs.workflow_runs) {
              if (run.id === currentRunId) continue;

              // 检查运行时间，15分钟内的视为可能重复
              const runTime = new Date(run.created_at);
              const now = new Date();
              const timeDiff = Math.abs(now - runTime);

              if (timeDiff < 15 * 60 * 1000) {
                console.log(`⚠️ 发现可能的重复运行 (run_id: ${run.id})`);
                console.log(`   创建时间: ${run.created_at}`);
                // 检查是否是同一个 issue（通过运行名称或其他方式）
                // 这里简单地认为 15 分钟内的重复触发需要跳过
                core.setOutput('is_duplicate', 'true');
                return;
              }
            }

            // 同时检查 issue 是否已经在处理中
            const { data: issue } = await github.rest.issues.get({
              owner: context.repo.owner,
              repo: context.repo.repo,
              issue_number: issueNumber
            });

            const labels = issue.labels.map(l => l.name);
            if (labels.includes('🤖 ai-processing')) {
              console.log(`⚠️ Issue #${issueNumber} 已在处理中，跳过`);
              core.setOutput('is_duplicate', 'true');
              return;
            }

            core.setOutput('is_duplicate', 'false');

      - name: Skip if Duplicate
        if: steps.check_duplicate.outputs.is_duplicate == 'true'
        run: |
          echo "⏭️ 检测到重复运行或 Issue 已在处理中，跳过本次执行"
          exit 0

      - name: Update Issue Status
        if: steps.check_duplicate.outputs.is_duplicate != 'true'
        id: update_issue_status
        uses: actions/github-script@v7
        with:
          script: |
            const issueNumber = ${{ steps.issue_info.outputs.issue_number }};

            // 更新标签
            try {
              await github.rest.issues.removeLabel({
                owner: context.repo.owner,
                repo: context.repo.repo,
                issue_number: issueNumber,
                name: 'complexity:medium'
              });
            } catch (e) {}

            await github.rest.issues.addLabels({
              owner: context.repo.owner,
              repo: context.repo.repo,
              issue_number: issueNumber,
              labels: ['🤖 ai-processing', 'agent:medium']
            });

            const continuationContext = '${{ github.event.inputs.continuation_context }}' || '';
            const isContinuation = continuationContext.length > 0;

            // 查找已有的进度追踪评论（vibe-continuous 创建的）
            let progressCommentId = null;
            let existingCommentBody = '';

            if (isContinuation) {
              // 获取 issue 的所有评论
              const { data: comments } = await github.rest.issues.listComments({
                owner: context.repo.owner,
                repo: context.repo.repo,
                issue_number: issueNumber,
                per_page: 100
              });

              // 查找最近的 vibe-continuous 进度评论（包含 "🔄 自动迭代"）
              for (let i = comments.length - 1; i >= 0; i--) {
                const comment = comments[i];
                if (comment.body && comment.body.includes('🔄 自动迭代')) {
                  progressCommentId = comment.id;
                  existingCommentBody = comment.body;
                  console.log(`找到进度追踪评论: #${progressCommentId}`);
                  break;
                }
              }
            }

            // 构建进度评论内容
            const progressBody = isContinuation ? [
              existingCommentBody || `🔄 **自动迭代**\n\n**上下文**: ${continuationContext}`,
              "",
              "---",
              "",
              "## 🤖 Medium Agent 执行中",
              "",
              "**当前阶段**: 🔄 准备中...",
              "",
              "```",
              "░░░░░░░░░░░░░░░░░░░░ 0%",
              "```",
              "",
              "**状态**: ⏳ 正在初始化...",
            ].join("\n") : [
              "🔧 **Medium Agent 启动**",
              "",
              "---",
              "",
              "## 📊 执行进度",
              "",
              "**当前阶段**: 🔄 准备中...",
              "",
              "```",
              "░░░░░░░░░░░░░░░░░░░░ 0%",
              "```",
              "",
              "**状态**: ⏳ 正在初始化...",
              "",
              "---",
              "> 💡 中等任务会先分析再开发，确保方案合理。"
            ].join("\n");

            // 更新或创建进度评论
            if (progressCommentId) {
              await github.rest.issues.updateComment({
                owner: context.repo.owner,
                repo: context.repo.repo,
                comment_id: progressCommentId,
                body: progressBody
              });
              console.log(`更新进度追踪评论: #${progressCommentId}`);
            } else {
              const progressComment = await github.rest.issues.createComment({
                owner: context.repo.owner,
                repo: context.repo.repo,
                issue_number: issueNumber,
                body: progressBody
              });
              progressCommentId = progressComment.data.id;
              console.log(`创建新的进度追踪评论: #${progressCommentId}`);
            }

            // 保存评论 ID 和原始内容供后续更新
            core.setOutput('progress_comment_id', progressCommentId);
            core.setOutput('existing_comment_body', existingCommentBody);

      - name: Update Progress - Loading Template
        uses: actions/github-script@v7
        with:
          script: |
            const issueNumber = ${{ steps.issue_info.outputs.issue_number }};
            const progressCommentId = '${{ steps.update_issue_status.outputs.progress_comment_id }}' || null;
            const continuationContext = '${{ github.event.inputs.continuation_context }}' || '';
            const isContinuation = continuationContext.length > 0;
            const existingCommentBody = `${{ steps.update_issue_status.outputs.existing_comment_body }}`;

            // 构建进度部分
            const progressSection = [
              "## 🤖 Medium Agent 执行中",
              "",
              "**当前阶段**: 📝 加载模板和准备提示词...",
              "",
              "```",
              "██░░░░░░░░░░░░░░░░░░ 10%",
              "```",
              "",
              "**状态**: ⏳ 正在加载开发模板...",
            ].join("\n");

            if (progressCommentId) {
              const body = isContinuation && existingCommentBody
                ? existingCommentBody.split('---')[0] + "---\n\n" + progressSection
                : [
                    "🔧 **Medium Agent 启动**",
                    "",
                    "---",
                    "",
                    "## 📊 执行进度",
                    "",
                    "**当前阶段**: 📝 加载模板和准备提示词...",
                    "",
                    "```",
                    "██░░░░░░░░░░░░░░░░░░ 10%",
                    "```",
                    "",
                    "**状态**: ⏳ 正在加载开发模板...",
                    "",
                    "---",
                    "> 💡 中等任务会先分析再开发，确保方案合理。"
                  ].join("\n");

              await github.rest.issues.updateComment({
                owner: context.repo.owner,
                repo: context.repo.repo,
                comment_id: progressCommentId,
                body: body
              });
            }

      - name: Load Prompt Template
        id: prompt
        uses: ./.github/actions/load-prompt
        with:
          template: agents/medium.md
          variables: |
            {
              "repository": "${{ github.repository }}",
              "issue_number": "${{ steps.issue_info.outputs.issue_number }}",
              "title": ${{ toJson(steps.issue_info.outputs.issue_title) }},
              "body": ${{ toJson(steps.issue_info.outputs.issue_body) }},
              "continuation_context": "${{ github.event.inputs.continuation_context || '' }}"
            }

      - name: Update Progress - Running Claude
        uses: actions/github-script@v7
        with:
          script: |
            const issueNumber = ${{ steps.issue_info.outputs.issue_number }};
            const progressCommentId = '${{ steps.update_issue_status.outputs.progress_comment_id }}' || null;
            const continuationContext = '${{ github.event.inputs.continuation_context }}' || '';
            const isContinuation = continuationContext.length > 0;
            const existingCommentBody = `${{ steps.update_issue_status.outputs.existing_comment_body }}`;

            // 构建进度部分
            const progressSection = [
              "## 🤖 Medium Agent 执行中",
              "",
              "**当前阶段**: 🤖 AI 正在分析和开发...",
              "",
              "```",
              "████████░░░░░░░░░░░░ 40%",
              "```",
              "",
              "**状态**: ⏳ Claude 正在处理任务...",
              "",
              "**提示**: 这可能需要几分钟时间，请耐心等待",
            ].join("\n");

            if (progressCommentId) {
              const body = isContinuation && existingCommentBody
                ? existingCommentBody.split('---')[0] + "---\n\n" + progressSection
                : [
                    "🔧 **Medium Agent 启动**",
                    "",
                    "---",
                    "",
                    "## 📊 执行进度",
                    "",
                    "**当前阶段**: 🤖 AI 正在分析和开发...",
                    "",
                    "```",
                    "████████░░░░░░░░░░░░ 40%",
                    "```",
                    "",
                    "**状态**: ⏳ Claude 正在处理任务...",
                    "",
                    "**提示**: 这可能需要几分钟时间，请耐心等待",
                    "",
                    "---",
                    "> 💡 中等任务会先分析再开发，确保方案合理。"
                  ].join("\n");

              await github.rest.issues.updateComment({
                owner: context.repo.owner,
                repo: context.repo.repo,
                comment_id: progressCommentId,
                body: body
              });
            }

      - name: Run Claude Code Action
        if: steps.check_duplicate.outputs.is_duplicate != 'true'
        id: claude
        uses: anthropics/claude-code-action@v1
        continue-on-error: true
        env:
          ANTHROPIC_BASE_URL: https://openrouter.ai/api
        with:
          anthropic_api_key: ${{ secrets.OPENROUTER_API_KEY }}
          github_token: ${{ github.token }}
          # track_progress only supported for issue_comment events, not workflow_dispatch
          track_progress: ${{ github.event_name == 'issue_comment' }}
          prompt: ${{ steps.prompt.outputs.prompt }}
          claude_args: |
            --model anthropic/claude-sonnet-4
            --max-turns 50
            --allowedTools "Bash,Read,Write,Edit,Glob,Grep,WebFetch"

      - name: Check and Handle Claude Result
        id: check_result
        run: |
          OUTCOME="${{ steps.claude.outcome }}"
          CONCLUSION="${{ steps.claude.conclusion }}"

          echo "Claude outcome: $OUTCOME"
          echo "Claude conclusion: $CONCLUSION"

          if [ "$OUTCOME" == "failure" ] || [ "$OUTCOME" == "cancelled" ]; then
            echo "claude_failed=true" >> $GITHUB_OUTPUT

            # 尝试从多个位置提取错误信息
            ERROR_MSG=""
            if [ -f "/home/runner/work/_temp/claude-execution-output.json" ]; then
              ERROR_MSG=$(cat /home/runner/work/_temp/claude-execution-output.json | jq -r '.error // .message // empty' 2>/dev/null || true)
            fi

            if [ -z "$ERROR_MSG" ] && [ -f "/home/runner/work/_temp/claude-error.log" ]; then
              ERROR_MSG=$(head -c 500 /home/runner/work/_temp/claude-error.log 2>/dev/null || true)
            fi

            if [ -z "$ERROR_MSG" ]; then
              if [ "$OUTCOME" == "cancelled" ]; then
                ERROR_MSG="Claude 执行被取消"
              else
                ERROR_MSG="执行失败，请查看 Actions 日志获取详细信息"
              fi
            fi

            echo "error_message<<EOF" >> $GITHUB_OUTPUT
            echo "$ERROR_MSG" >> $GITHUB_OUTPUT
            echo "EOF" >> $GITHUB_OUTPUT
          else
            echo "claude_failed=false" >> $GITHUB_OUTPUT
          fi

      - name: Exit on Claude Failure
        if: steps.check_result.outputs.claude_failed == 'true'
        run: |
          echo "Claude execution failed: ${{ steps.check_result.outputs.error_message }}"
          exit 1

      - name: Update Issue on Success
        if: success() && steps.check_duplicate.outputs.is_duplicate != 'true'
        uses: actions/github-script@v7
        with:
          script: |
            const issueNumber = ${{ steps.issue_info.outputs.issue_number }};
            const progressCommentId = '${{ steps.update_issue_status.outputs.progress_comment_id }}' || null;
            const continuationContext = '${{ github.event.inputs.continuation_context }}' || '';
            const isContinuation = continuationContext.length > 0;
            const existingCommentBody = `${{ steps.update_issue_status.outputs.existing_comment_body }}`;

            // 获取最近创建的 PR（由本次执行创建）
            let prInfo = '';
            let relatedPR = null;
            try {
              // 获取最近的 PR（包括 open 和刚合并的）
              const { data: prs } = await github.rest.pulls.list({
                owner: context.repo.owner,
                repo: context.repo.repo,
                state: 'all',
                sort: 'created',
                direction: 'desc',
                per_page: 10
              });

              // 查找标题或分支包含 issue 编号的 PR
              relatedPR = prs.find(pr =>
                pr.title.includes(`#${issueNumber}`) ||
                pr.title.toLowerCase().includes(`issue-${issueNumber}`) ||
                pr.head.ref.includes(`issue-${issueNumber}`) ||
                pr.head.ref.includes(`${issueNumber}`) ||
                pr.body?.includes(`#${issueNumber}`) ||
                pr.body?.includes(`Closes #${issueNumber}`) ||
                pr.body?.includes(`Fixes #${issueNumber}`)
              );

              if (relatedPR) {
                prInfo = `\n\n**创建的 PR**: [#${relatedPR.number} ${relatedPR.title}](${relatedPR.html_url})`;
                console.log(`✅ 找到关联 PR: #${relatedPR.number}`);
              } else {
                console.log(`⚠️ 未找到关联 PR`);
              }
            } catch (e) {
              console.log("获取 PR 信息失败:", e.message);
            }

            // 关键检查：如果没有找到 PR，不能关闭 Issue
            if (!relatedPR) {
              console.log(`❌ 未找到关联 PR，不关闭 Issue #${issueNumber}`);

              // 构建警告状态
              const warningSection = [
                "## ⚠️ Medium Agent 完成但未创建 PR",
                "",
                "**当前阶段**: ⚠️ 代码未提交",
                "",
                "```",
                "████████████████░░░░ 80%",
                "```",
                "",
                "**状态**: ⚠️ 代码可能已写入但未创建 PR",
                "",
                "**问题**: Agent 执行完成，但未检测到关联的 Pull Request。",
                "",
                "**可能原因**:",
                "- Agent 未执行 git commit/push",
                "- Agent 未创建 PR",
                "- PR 创建失败",
                "",
                "**建议**: 请检查 Actions 日志，手动创建 PR 或重新触发 Agent。",
                "",
                `**Actions 日志**: [查看详情](/${context.repo.owner}/${context.repo.repo}/actions/runs/${{ github.run_id }})`
              ].join("\n");

              // 更新进度评论为警告状态
              if (progressCommentId) {
                try {
                  const body = isContinuation && existingCommentBody
                    ? existingCommentBody.split('---')[0] + "---\n\n" + warningSection
                    : [
                        "🔧 **Medium Agent**",
                        "",
                        "---",
                        "",
                        warningSection
                      ].join("\n");

                  await github.rest.issues.updateComment({
                    owner: context.repo.owner,
                    repo: context.repo.repo,
                    comment_id: progressCommentId,
                    body: body
                  });
                } catch (e) {
                  console.log("无法更新进度评论:", e.message);
                }
              }

              // 移除 processing 标签，添加警告标签
              try {
                await github.rest.issues.removeLabel({
                  owner: context.repo.owner,
                  repo: context.repo.repo,
                  issue_number: issueNumber,
                  name: '🤖 ai-processing'
                });
              } catch (e) {}

              try {
                await github.rest.issues.removeLabel({
                  owner: context.repo.owner,
                  repo: context.repo.repo,
                  issue_number: issueNumber,
                  name: 'agent:medium'
                });
              } catch (e) {}

              await github.rest.issues.addLabels({
                owner: context.repo.owner,
                repo: context.repo.repo,
                issue_number: issueNumber,
                labels: ['⚠️ no-pr', 'needs-review']
              });

              // 不关闭 Issue，直接返回
              return;
            }

            // 有 PR 的情况：正常完成流程
            // 构建完成状态
            const completionSection = [
              "## ✅ Medium Agent 完成",
              "",
              "**当前阶段**: ✅ 完成",
              "",
              "```",
              "████████████████████ 100%",
              "```",
              "",
              "**状态**: ✅ 需求分析和代码实现已完成",
              prInfo,
              "",
              `**Actions 日志**: [查看详情](/${context.repo.owner}/${context.repo.repo}/actions/runs/${{ github.run_id }})`
            ].join("\n");

            // 更新进度评论为完成状态
            if (progressCommentId) {
              try {
                const body = isContinuation && existingCommentBody
                  ? existingCommentBody.split('---')[0] + "---\n\n" + completionSection
                  : [
                      "🔧 **Medium Agent**",
                      "",
                      "---",
                      "",
                      completionSection
                    ].join("\n");

                await github.rest.issues.updateComment({
                  owner: context.repo.owner,
                  repo: context.repo.repo,
                  comment_id: progressCommentId,
                  body: body
                });
              } catch (e) {
                console.log("无法更新进度评论:", e.message);
              }
            }

            // 移除 processing 标签，添加 completed 标签
            try {
              await github.rest.issues.removeLabel({
                owner: context.repo.owner,
                repo: context.repo.repo,
                issue_number: issueNumber,
                name: '🤖 ai-processing'
              });
            } catch (e) {}

            try {
              await github.rest.issues.removeLabel({
                owner: context.repo.owner,
                repo: context.repo.repo,
                issue_number: issueNumber,
                name: 'agent:medium'
              });
            } catch (e) {}

            await github.rest.issues.addLabels({
              owner: context.repo.owner,
              repo: context.repo.repo,
              issue_number: issueNumber,
              labels: ['✅ ai-completed']
            });

            // 只有找到 PR 才关闭 Issue
            await github.rest.issues.update({
              owner: context.repo.owner,
              repo: context.repo.repo,
              issue_number: issueNumber,
              state: 'closed',
              state_reason: 'completed'
            });

            console.log(`✅ Issue #${issueNumber} 已自动关闭，将触发依赖链中的下一个任务`);

      - name: Trigger Verification
        if: success()
        uses: actions/github-script@v7
        with:
          script: |
            const issueNumber = ${{ steps.issue_info.outputs.issue_number }};

            console.log(`🔍 触发验收检查: Issue #${issueNumber}`);

            try {
              await github.rest.actions.createWorkflowDispatch({
                owner: context.repo.owner,
                repo: context.repo.repo,
                workflow_id: 'vibe-continuous.yml',
                ref: 'main',
                inputs: {
                  issue_number: String(issueNumber),
                  mode: 'verify'
                }
              });
              console.log(`✅ 已触发 vibe-continuous 验收检查`);
            } catch (error) {
              console.log(`⚠️ 触发验收检查失败: ${error.message}`);
              // 即使触发失败也不影响整体流程
            }

      - name: Update Issue on Failure
        if: failure() && steps.check_duplicate.outputs.is_duplicate != 'true'
        uses: actions/github-script@v7
        with:
          script: |
            const issueNumber = ${{ steps.issue_info.outputs.issue_number }};
            const progressCommentId = '${{ steps.update_issue_status.outputs.progress_comment_id }}' || null;
            const continuationContext = '${{ github.event.inputs.continuation_context }}' || '';
            const isContinuation = continuationContext.length > 0;
            const existingCommentBody = `${{ steps.update_issue_status.outputs.existing_comment_body }}`;
            const errorMessage = `${{ steps.check_result.outputs.error_message || '未知错误' }}`;

            // 分析错误类型并给出针对性建议
            let errorAnalysis = "";
            let suggestion = "";

            if (errorMessage.includes("Edit") || errorMessage.includes("old_string") || errorMessage.includes("not found")) {
              errorAnalysis = "代码编辑失败 - 目标代码块未找到（可能已被修改）";
              suggestion = "检查目标文件是否已更新，或手动完成此任务";
            } else if (errorMessage.includes("timeout") || errorMessage.includes("Timeout")) {
              errorAnalysis = "执行超时 - 任务过于复杂";
              suggestion = "使用 `/agent-complex` 将任务拆分为多个子任务";
            } else if (errorMessage.includes("rate limit") || errorMessage.includes("429") || errorMessage.includes("Too Many Requests")) {
              errorAnalysis = "API 请求限流";
              suggestion = "等待几分钟后使用 `/agent-medium` 重试";
            } else if (errorMessage.includes("permission") || errorMessage.includes("Permission")) {
              errorAnalysis = "权限不足";
              suggestion = "检查 GitHub Token 权限配置";
            } else if (errorMessage.includes("build") || errorMessage.includes("compile")) {
              errorAnalysis = "构建失败 - 代码存在语法或编译错误";
              suggestion = "查看 Actions 日志定位具体错误";
            } else {
              errorAnalysis = "执行过程中出现错误";
              suggestion = "查看 Actions 日志获取详细信息";
            }

            // 构建失败状态
            const failureSection = [
              "## ❌ Medium Agent 失败",
              "",
              "**当前阶段**: ❌ 失败",
              "",
              "```",
              "████████░░░░░░░░░░░░ 40%",
              "```",
              "",
              `**错误类型**: ${errorAnalysis}`,
              "",
              `**Actions 日志**: [查看详情](/${context.repo.owner}/${context.repo.repo}/actions/runs/${{ github.run_id }})`,
              "",
              "**建议:**",
              `- ${suggestion}`,
              "- 或使用 `/agent-medium` 重试",
              "- 或使用 `/agent-complex` 让 AI 先拆分任务",
              "",
              "<details>",
              "<summary>错误详情</summary>",
              "",
              "```",
              errorMessage.substring(0, 500),
              "```",
              "</details>"
            ].join("\n");

            // 更新进度评论为失败状态
            if (progressCommentId) {
              try {
                const body = isContinuation && existingCommentBody
                  ? existingCommentBody.split('---')[0] + "---\n\n" + failureSection
                  : [
                      "🔧 **Medium Agent**",
                      "",
                      "---",
                      "",
                      failureSection
                    ].join("\n");

                await github.rest.issues.updateComment({
                  owner: context.repo.owner,
                  repo: context.repo.repo,
                  comment_id: progressCommentId,
                  body: body
                });
              } catch (e) {
                console.log("无法更新进度评论:", e.message);
              }
            }

            // 移除 processing 标签
            try {
              await github.rest.issues.removeLabel({
                owner: context.repo.owner,
                repo: context.repo.repo,
                issue_number: issueNumber,
                name: '🤖 ai-processing'
              });
            } catch (e) {}

            try {
              await github.rest.issues.removeLabel({
                owner: context.repo.owner,
                repo: context.repo.repo,
                issue_number: issueNumber,
                name: 'agent:medium'
              });
            } catch (e) {}

            await github.rest.issues.addLabels({
              owner: context.repo.owner,
              repo: context.repo.repo,
              issue_number: issueNumber,
              labels: ['❌ ai-failed', 'needs-review']
            });

            // 不再创建额外评论，所有信息都在进度追踪评论中
