name: Complex Task Agent

on:
  workflow_call:
  issue_comment:
    types: [created]

jobs:
  complex-task:
    # 通过 workflow_call 或 /agent-complex 命令触发
    if: |
      github.event_name == 'workflow_call' ||
      contains(github.event.comment.body, '/agent-complex')
    runs-on: ubuntu-latest

    permissions:
      issues: write
      contents: read

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Update Issue Status
        uses: actions/github-script@v7
        with:
          script: |
            const issue = context.payload.issue;

            // 更新标签
            try {
              await github.rest.issues.removeLabel({
                owner: context.repo.owner,
                repo: context.repo.repo,
                issue_number: issue.number,
                name: 'complexity:complex'
              });
            } catch (e) {}

            await github.rest.issues.addLabels({
              owner: context.repo.owner,
              repo: context.repo.repo,
              issue_number: issue.number,
              labels: ['🤖 ai-processing', 'agent:complex', 'epic']
            });

            // 发布开始分析的评论
            await github.rest.issues.createComment({
              owner: context.repo.owner,
              repo: context.repo.repo,
              issue_number: issue.number,
              body: [
                "📋 **Complex Agent 启动**",
                "",
                "复杂任务将被拆分为多个子 Issue...",
                "",
                "**执行计划:**",
                "1. 分析需求并识别模块",
                "2. 拆分为可独立开发的子任务",
                "3. 创建子 Issue 并设置依赖关系",
                "4. 按依赖顺序自动触发开发",
                "",
                "> 💡 复杂任务需要拆分后才能高效处理。"
              ].join("\n")
            });

      - name: Analyze and Create Sub-Issues
        uses: actions/github-script@v7
        env:
          OPENROUTER_API_KEY: ${{ secrets.OPENROUTER_API_KEY }}
        with:
          script: |
            const issue = context.payload.issue;
            const title = issue.title || '';
            const body = issue.body || '';

            console.log("=".repeat(60));
            console.log("📋 COMPLEX AGENT - 任务拆分");
            console.log("=".repeat(60));

            // 使用 AI 分析并拆分任务
            const prompt = `你是一个资深技术架构师，擅长将复杂需求拆分为可执行的子任务。

            ## 原始需求
            标题: ${title}
            描述:
            ${body.substring(0, 4000)}

            ## 项目技术栈
            - 前端: Next.js 15 + React 19 + TypeScript + shadcn/ui
            - 后端: Go + Gin + GORM
            - 数据库: PostgreSQL

            ## 任务拆分原则
            1. 每个子任务应该可以独立开发和测试
            2. 子任务之间的依赖关系要明确
            3. 按照数据库 → 后端 → 前端的顺序排列
            4. 每个子任务预估 2-8 小时工作量
            5. 子任务数量控制在 3-8 个

            ## 输出格式
            返回 JSON 数组，每个子任务包含：
            {
              "tasks": [
                {
                  "title": "子任务标题",
                  "description": "详细描述，包含具体要实现的内容",
                  "type": "database" | "backend" | "frontend" | "fullstack",
                  "priority": 1-5 (1最高),
                  "depends_on": [前置任务的序号，从0开始],
                  "estimated_hours": 预估小时数,
                  "acceptance_criteria": ["验收标准1", "验收标准2"]
                }
              ],
              "architecture_notes": "架构说明和注意事项"
            }

            只返回 JSON，不要其他内容。`;

            try {
              const response = await fetch("https://openrouter.ai/api/v1/chat/completions", {
                method: "POST",
                headers: {
                  "Authorization": `Bearer ${process.env.OPENROUTER_API_KEY}`,
                  "Content-Type": "application/json"
                },
                body: JSON.stringify({
                  model: "anthropic/claude-sonnet-4",
                  messages: [{ role: "user", content: prompt }],
                  temperature: 0.2
                })
              });

              if (!response.ok) {
                throw new Error(`API 请求失败: ${response.status}`);
              }

              const data = await response.json();
              let result = data.choices?.[0]?.message?.content || '';

              // 提取 JSON
              const jsonMatch = result.match(/\{[\s\S]*\}/);
              if (!jsonMatch) {
                throw new Error("无法解析 AI 响应");
              }

              const parsed = JSON.parse(jsonMatch[0]);
              const tasks = parsed.tasks || [];
              const architectureNotes = parsed.architecture_notes || '';

              console.log(`✅ 识别到 ${tasks.length} 个子任务`);

              if (tasks.length === 0) {
                throw new Error("未能拆分出子任务");
              }

              // 创建子 Issues
              const createdIssues = [];
              const typeLabels = {
                'database': ['database', 'backend'],
                'backend': ['backend'],
                'frontend': ['frontend'],
                'fullstack': ['frontend', 'backend']
              };

              const typeAgents = {
                'database': 'agent-medium',
                'backend': 'agent-medium',
                'frontend': 'agent-medium',
                'fullstack': 'agent-medium'
              };

              for (let i = 0; i < tasks.length; i++) {
                const task = tasks[i];

                // 构建依赖说明
                let dependsText = '';
                if (task.depends_on && task.depends_on.length > 0) {
                  const depTitles = task.depends_on.map(idx => {
                    if (createdIssues[idx]) {
                      return `#${createdIssues[idx].number}`;
                    }
                    return `任务 ${idx + 1}`;
                  });
                  dependsText = `\n\n**前置依赖:** ${depTitles.join(', ')}`;
                }

                // 验收标准
                const criteriaText = task.acceptance_criteria?.length > 0
                  ? `\n\n**验收标准:**\n${task.acceptance_criteria.map(c => `- [ ] ${c}`).join('\n')}`
                  : '';

                const subIssueBody = [
                  `## 🔗 关联`,
                  ``,
                  `Parent: #${issue.number}`,
                  `任务序号: ${i + 1}/${tasks.length}`,
                  dependsText,
                  ``,
                  `---`,
                  ``,
                  `## 📋 任务描述`,
                  ``,
                  task.description,
                  criteriaText,
                  ``,
                  `---`,
                  ``,
                  `## 📊 任务信息`,
                  ``,
                  `- **类型:** ${task.type}`,
                  `- **优先级:** P${task.priority}`,
                  `- **预估工时:** ${task.estimated_hours} 小时`,
                  ``,
                  `---`,
                  ``,
                  `> 🤖 此 Issue 由 Complex Agent 自动创建`,
                  `> 完成前置依赖后，使用 \`/agent-medium\` 或 \`/agent-simple\` 触发开发`
                ].filter(Boolean).join('\n');

                const labels = [
                  'sub-issue',
                  `priority:P${task.priority}`,
                  ...(typeLabels[task.type] || [])
                ];

                // 根据预估工时决定复杂度标签
                if (task.estimated_hours <= 2) {
                  labels.push('complexity:simple');
                } else if (task.estimated_hours <= 6) {
                  labels.push('complexity:medium');
                } else {
                  labels.push('complexity:complex');
                }

                const { data: subIssue } = await github.rest.issues.create({
                  owner: context.repo.owner,
                  repo: context.repo.repo,
                  title: `[${task.type.toUpperCase()}] ${task.title}`,
                  body: subIssueBody,
                  labels: labels
                });

                createdIssues.push({
                  number: subIssue.number,
                  title: task.title,
                  type: task.type,
                  priority: task.priority,
                  depends_on: task.depends_on || []
                });

                console.log(`✅ 创建子 Issue #${subIssue.number}: ${task.title}`);
              }

              // 在父 Issue 发布汇总评论
              const summaryTable = createdIssues.map((iss, idx) => {
                const deps = iss.depends_on.length > 0
                  ? iss.depends_on.map(d => `#${createdIssues[d]?.number || '?'}`).join(', ')
                  : '-';
                return `| ${idx + 1} | #${iss.number} | ${iss.title} | ${iss.type} | P${iss.priority} | ${deps} |`;
              }).join('\n');

              await github.rest.issues.createComment({
                owner: context.repo.owner,
                repo: context.repo.repo,
                issue_number: issue.number,
                body: [
                  "✅ **任务拆分完成**",
                  "",
                  `共创建 **${createdIssues.length}** 个子 Issue：`,
                  "",
                  "| # | Issue | 标题 | 类型 | 优先级 | 依赖 |",
                  "|---|-------|------|------|--------|------|",
                  summaryTable,
                  "",
                  architectureNotes ? `**架构说明:**\n${architectureNotes}\n` : '',
                  "---",
                  "",
                  "**下一步操作:**",
                  "1. 检查子 Issue 的拆分是否合理",
                  "2. 从没有依赖的 Issue 开始（优先级高的先做）",
                  "3. 在子 Issue 中使用 `/agent-medium` 或 `/agent-simple` 触发开发",
                  "",
                  "> 💡 建议按照 数据库 → 后端 → 前端 的顺序开发",
                  "",
                  "---",
                  "> 🤖 由 Vibe Complex Agent 自动处理"
                ].filter(Boolean).join("\n")
              });

              // 更新标签
              try {
                await github.rest.issues.removeLabel({
                  owner: context.repo.owner,
                  repo: context.repo.repo,
                  issue_number: issue.number,
                  name: '🤖 ai-processing'
                });
              } catch (e) {}

              await github.rest.issues.addLabels({
                owner: context.repo.owner,
                repo: context.repo.repo,
                issue_number: issue.number,
                labels: ['✅ ai-completed', 'has-subtasks']
              });

              // 自动触发第一个无依赖的子任务
              const firstTask = createdIssues.find(iss => !iss.depends_on || iss.depends_on.length === 0);
              if (firstTask) {
                await github.rest.issues.createComment({
                  owner: context.repo.owner,
                  repo: context.repo.repo,
                  issue_number: firstTask.number,
                  body: `/agent-medium\n\n---\n\n🤖 **自动触发开发**\n\n此任务无前置依赖，自动开始开发。`
                });
                console.log(`🚀 自动触发子任务 #${firstTask.number}`);
              }

            } catch (error) {
              console.error("❌ 任务拆分失败:", error.message);

              await github.rest.issues.createComment({
                owner: context.repo.owner,
                repo: context.repo.repo,
                issue_number: issue.number,
                body: [
                  "❌ **Complex Agent 失败**",
                  "",
                  `错误: ${error.message}`,
                  "",
                  "**可能的原因:**",
                  "- 需求描述不够详细",
                  "- 需求范围过大或过小",
                  "- API 调用失败",
                  "",
                  "**解决方案:**",
                  "- 补充需求描述（包含具体功能点）",
                  "- 使用 `/agent-complex` 重试",
                  "- 或手动拆分需求后使用 `/agent-medium`",
                  "",
                  "---",
                  "> 🤖 由 Vibe Complex Agent 处理"
                ].join("\n")
              });

              try {
                await github.rest.issues.removeLabel({
                  owner: context.repo.owner,
                  repo: context.repo.repo,
                  issue_number: issue.number,
                  name: '🤖 ai-processing'
                });
              } catch (e) {}

              await github.rest.issues.addLabels({
                owner: context.repo.owner,
                repo: context.repo.repo,
                issue_number: issue.number,
                labels: ['❌ ai-failed', 'needs-review']
              });

              core.setFailed(error.message);
            }
