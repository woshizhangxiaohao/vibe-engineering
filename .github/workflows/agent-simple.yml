name: Simple Task Agent

on:
  workflow_call:
  workflow_dispatch:
    inputs:
      issue_number:
        description: 'Issue 编号'
        required: true
        type: string
      continuation_context:
        description: '继续迭代的上下文信息'
        required: false
        type: string
  issue_comment:
    types: [created]

jobs:
  simple-task:
    # 通过 workflow_call、workflow_dispatch 或 /agent-simple 命令触发
    if: |
      github.event_name == 'workflow_call' ||
      github.event_name == 'workflow_dispatch' ||
      (github.event_name == 'issue_comment' &&
        github.event.issue.pull_request == null &&
        contains(github.event.comment.body, '/agent-simple'))
    runs-on: ubuntu-latest

    permissions:
      issues: write
      contents: write
      pull-requests: write
      checks: read

    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Setup Git
        run: |
          git config user.name "vibe-simple-agent"
          git config user.email "agent@vibe.bot"

      - name: Get Issue Info
        id: issue_info
        uses: actions/github-script@v7
        with:
          script: |
            let issueNumber;
            let issue;

            if (context.eventName === 'workflow_dispatch') {
              // workflow_dispatch 触发时从 inputs 获取
              issueNumber = parseInt('${{ github.event.inputs.issue_number }}');
              const { data } = await github.rest.issues.get({
                owner: context.repo.owner,
                repo: context.repo.repo,
                issue_number: issueNumber
              });
              issue = data;
            } else {
              // issue_comment 或 workflow_call 触发
              issue = context.payload.issue;
              issueNumber = issue.number;
            }

            core.setOutput('issue_number', issueNumber);
            core.setOutput('issue_title', issue.title);
            core.setOutput('issue_body', issue.body || '');

            return { issueNumber, title: issue.title, body: issue.body };

      - name: Check for Duplicate Run
        id: check_duplicate
        uses: actions/github-script@v7
        with:
          script: |
            const issueNumber = ${{ steps.issue_info.outputs.issue_number }};
            const currentRunId = context.runId;

            // 检查是否有同一个 issue 的 agent 正在运行
            const { data: runs } = await github.rest.actions.listWorkflowRuns({
              owner: context.repo.owner,
              repo: context.repo.repo,
              workflow_id: 'agent-simple.yml',
              status: 'in_progress',
              per_page: 20
            });

            // 查找其他正在运行的相同 issue 任务
            for (const run of runs.workflow_runs) {
              if (run.id === currentRunId) continue;

              // 检查运行时间，15分钟内的视为可能重复
              const runTime = new Date(run.created_at);
              const now = new Date();
              const timeDiff = Math.abs(now - runTime);

              if (timeDiff < 15 * 60 * 1000) {
                console.log(`⚠️ 发现可能的重复运行 (run_id: ${run.id})`);
                console.log(`   创建时间: ${run.created_at}`);
                core.setOutput('is_duplicate', 'true');
                return;
              }
            }

            // 同时检查 issue 是否已经在处理中
            const { data: issue } = await github.rest.issues.get({
              owner: context.repo.owner,
              repo: context.repo.repo,
              issue_number: issueNumber
            });

            const labels = issue.labels.map(l => l.name);
            if (labels.includes('🤖 ai-processing')) {
              console.log(`⚠️ Issue #${issueNumber} 已在处理中，跳过`);
              core.setOutput('is_duplicate', 'true');
              return;
            }

            core.setOutput('is_duplicate', 'false');

      - name: Skip if Duplicate
        if: steps.check_duplicate.outputs.is_duplicate == 'true'
        run: |
          echo "⏭️ 检测到重复运行或 Issue 已在处理中，跳过本次执行"
          exit 0

      - name: Update Issue Status
        if: steps.check_duplicate.outputs.is_duplicate != 'true'
        id: update_issue_status
        uses: actions/github-script@v7
        with:
          script: |
            const issueNumber = ${{ steps.issue_info.outputs.issue_number }};

            // 更新标签
            try {
              await github.rest.issues.removeLabel({
                owner: context.repo.owner,
                repo: context.repo.repo,
                issue_number: issueNumber,
                name: 'complexity:simple'
              });
            } catch (e) {}

            await github.rest.issues.addLabels({
              owner: context.repo.owner,
              repo: context.repo.repo,
              issue_number: issueNumber,
              labels: ['🤖 ai-processing', 'agent:simple']
            });
            
            // 创建进度追踪评论
            const progressComment = await github.rest.issues.createComment({
              owner: context.repo.owner,
              repo: context.repo.repo,
              issue_number: issueNumber,
              body: [
                "⚡ **Simple Agent 启动**",
                "",
                "---",
                "",
                "## 📊 执行进度",
                "",
                "**当前阶段**: 🔄 准备中...",
                "",
                "```",
                "░░░░░░░░░░░░░░░░░░░░ 0%",
                "```",
                "",
                "**状态**: ⏳ 正在初始化...",
                "",
                "---",
                "> 💡 简单任务将直接开始开发。"
              ].join("\n")
            });
            
            // 保存评论 ID 供后续更新
            core.setOutput('progress_comment_id', progressComment.data.id);

      - name: Update Progress - Loading Template
        if: steps.check_duplicate.outputs.is_duplicate != 'true'
        uses: actions/github-script@v7
        with:
          script: |
            const issueNumber = ${{ steps.issue_info.outputs.issue_number }};
            const progressCommentId = '${{ steps.update_issue_status.outputs.progress_comment_id }}' || null;
            
            // 进度条生成函数
            function generateProgressBar(percentage) {
              const totalBlocks = 20;
              const filledBlocks = Math.round((percentage / 100) * totalBlocks);
              const emptyBlocks = totalBlocks - filledBlocks;
              return '█'.repeat(filledBlocks) + '░'.repeat(emptyBlocks) + ` ${percentage}%`;
            }

            if (progressCommentId) {
              await github.rest.issues.updateComment({
                owner: context.repo.owner,
                repo: context.repo.repo,
                comment_id: progressCommentId,
                body: [
                  "⚡ **Simple Agent 启动**",
                  "",
                  "---",
                  "",
                  "## 📊 执行进度",
                  "",
                  "**当前阶段**: 📝 加载模板和准备提示词...",
                  "",
                  "```",
                  generateProgressBar(10),
                  "```",
                  "",
                  "**状态**: ⏳ 正在加载开发模板...",
                  "",
                  "---",
                  "> 💡 简单任务将直接开始开发。"
                ].join("\n")
              });
            }

      - name: Load Prompt Template
        if: steps.check_duplicate.outputs.is_duplicate != 'true'
        id: prompt
        uses: ./.github/actions/load-prompt
        with:
          template: agents/simple.md
          variables: |
            {
              "repository": "${{ github.repository }}",
              "issue_number": "${{ steps.issue_info.outputs.issue_number }}",
              "title": ${{ toJson(steps.issue_info.outputs.issue_title) }},
              "body": ${{ toJson(steps.issue_info.outputs.issue_body) }},
              "continuation_context": "${{ github.event.inputs.continuation_context || '' }}"
            }

      - name: Update Progress - Running Claude
        if: steps.check_duplicate.outputs.is_duplicate != 'true'
        uses: actions/github-script@v7
        with:
          script: |
            const issueNumber = ${{ steps.issue_info.outputs.issue_number }};
            const progressCommentId = '${{ steps.update_issue_status.outputs.progress_comment_id }}' || null;
            
            // 进度条生成函数
            function generateProgressBar(percentage) {
              const totalBlocks = 20;
              const filledBlocks = Math.round((percentage / 100) * totalBlocks);
              const emptyBlocks = totalBlocks - filledBlocks;
              return '█'.repeat(filledBlocks) + '░'.repeat(emptyBlocks) + ` ${percentage}%`;
            }

            if (progressCommentId) {
              await github.rest.issues.updateComment({
                owner: context.repo.owner,
                repo: context.repo.repo,
                comment_id: progressCommentId,
                body: [
                  "⚡ **Simple Agent 启动**",
                  "",
                  "---",
                  "",
                  "## 📊 执行进度",
                  "",
                  "**当前阶段**: 🤖 AI 正在开发...",
                  "",
                  "```",
                  generateProgressBar(50),
                  "```",
                  "",
                  "**状态**: ⏳ Claude 正在处理任务...",
                  "",
                  "**提示**: 这可能需要几分钟时间，请耐心等待",
                  "",
                  "---",
                  "> 💡 简单任务将直接开始开发。"
                ].join("\n")
              });
            }

      - name: Run Claude Code Action
        if: steps.check_duplicate.outputs.is_duplicate != 'true'
        id: claude
        uses: anthropics/claude-code-action@v1
        continue-on-error: true
        env:
          ANTHROPIC_BASE_URL: https://openrouter.ai/api
        with:
          anthropic_api_key: ${{ secrets.OPENROUTER_API_KEY }}
          github_token: ${{ github.token }}
          # track_progress only supports: pull_request, issues, issue_comment, pull_request_review_comment, pull_request_review
          # Not supported for workflow_dispatch or workflow_call events
          track_progress: ${{ github.event_name == 'issue_comment' }}
          prompt: ${{ steps.prompt.outputs.prompt }}
          # 允许所有 bots 触发
          # 这对于以下自动化场景是必需的：
          # - vibe-router 自动路由任务
          # - vibe-continuous 自动迭代和验收
          # - dependency-chain 触发依赖任务
          # 否则会报错：Workflow initiated by non-human actor
          allowed_bots: '*'
          claude_args: |
            --model anthropic/claude-sonnet-4
            --max-turns 30

      - name: Check and Handle Claude Result
        if: steps.check_duplicate.outputs.is_duplicate != 'true'
        id: check_result
        run: |
          OUTCOME="${{ steps.claude.outcome }}"
          CONCLUSION="${{ steps.claude.conclusion }}"
          BRANCH_NAME="${{ steps.claude.outputs.branch_name }}"

          echo "Claude outcome: $OUTCOME"
          echo "Claude conclusion: $CONCLUSION"
          echo "Branch name: $BRANCH_NAME"

          # 保存分支名供后续使用
          echo "branch_name=$BRANCH_NAME" >> $GITHUB_OUTPUT

          if [ "$OUTCOME" == "failure" ] || [ "$OUTCOME" == "cancelled" ]; then
            echo "claude_failed=true" >> $GITHUB_OUTPUT

            # 尝试从多个位置提取错误信息
            ERROR_MSG=""
            if [ -f "/home/runner/work/_temp/claude-execution-output.json" ]; then
              ERROR_MSG=$(cat /home/runner/work/_temp/claude-execution-output.json | jq -r '.error // .message // empty' 2>/dev/null || true)
            fi

            if [ -z "$ERROR_MSG" ] && [ -f "/home/runner/work/_temp/claude-error.log" ]; then
              ERROR_MSG=$(head -c 500 /home/runner/work/_temp/claude-error.log 2>/dev/null || true)
            fi

            if [ -z "$ERROR_MSG" ]; then
              if [ "$OUTCOME" == "cancelled" ]; then
                ERROR_MSG="Claude 执行被取消"
              else
                ERROR_MSG="执行失败，请查看 Actions 日志获取详细信息"
              fi
            fi

            echo "error_message<<EOF" >> $GITHUB_OUTPUT
            echo "$ERROR_MSG" >> $GITHUB_OUTPUT
            echo "EOF" >> $GITHUB_OUTPUT
          else
            echo "claude_failed=false" >> $GITHUB_OUTPUT
          fi

      - name: Exit on Claude Failure
        if: steps.check_duplicate.outputs.is_duplicate != 'true' && steps.check_result.outputs.claude_failed == 'true'
        run: |
          echo "Claude execution failed: ${{ steps.check_result.outputs.error_message }}"
          exit 1

      - name: Update Issue on Success
        if: success() && steps.check_duplicate.outputs.is_duplicate != 'true'
        uses: actions/github-script@v7
        with:
          script: |
            const issueNumber = ${{ steps.issue_info.outputs.issue_number }};
            const progressCommentId = '${{ steps.update_issue_status.outputs.progress_comment_id }}' || null;
            const branchName = '${{ steps.check_result.outputs.branch_name }}' || '';

            // 获取最近创建的 PR（由本次执行创建）
            let prInfo = '';
            let relatedPR = null;
            try {
              const { data: prs } = await github.rest.pulls.list({
                owner: context.repo.owner,
                repo: context.repo.repo,
                state: 'all',
                sort: 'created',
                direction: 'desc',
                per_page: 10
              });

              relatedPR = prs.find(pr =>
                pr.title.includes(`#${issueNumber}`) ||
                pr.title.toLowerCase().includes(`issue-${issueNumber}`) ||
                pr.head.ref.includes(`issue-${issueNumber}`) ||
                pr.head.ref.includes(`${issueNumber}`) ||
                (branchName && pr.head.ref === branchName) ||
                pr.body?.includes(`#${issueNumber}`) ||
                pr.body?.includes(`Closes #${issueNumber}`) ||
                pr.body?.includes(`Fixes #${issueNumber}`)
              );

              if (relatedPR) {
                prInfo = `\n\n**创建的 PR**: [#${relatedPR.number} ${relatedPR.title}](${relatedPR.html_url})`;
                console.log(`✅ 找到关联 PR: #${relatedPR.number}`);
              } else {
                console.log(`⚠️ 未找到关联 PR`);
              }
            } catch (e) {
              console.log("获取 PR 信息失败:", e.message);
            }

            // 关键检查：如果没有找到 PR，不能关闭 Issue
            if (!relatedPR) {
              console.log(`❌ 未找到关联 PR，不关闭 Issue #${issueNumber}`);

              // 进度条生成函数
              function generateProgressBar(percentage) {
                const totalBlocks = 20;
                const filledBlocks = Math.round((percentage / 100) * totalBlocks);
                const emptyBlocks = totalBlocks - filledBlocks;
                return '█'.repeat(filledBlocks) + '░'.repeat(emptyBlocks) + ` ${percentage}%`;
              }

              // 构建 claude-code-action 风格的警告状态
              const jobUrl = `https://github.com/${context.repo.owner}/${context.repo.repo}/actions/runs/${{ github.run_id }}`;
              const statusLine = `**⚠️ Claude finished** — No PR created —— [View job](${jobUrl})`;

              if (progressCommentId) {
                try {
                  await github.rest.issues.updateComment({
                    owner: context.repo.owner,
                    repo: context.repo.repo,
                    comment_id: progressCommentId,
                    body: [
                      "⚡ **Simple Agent**",
                      "",
                      "---",
                      "",
                      statusLine,
                      "",
                      "```",
                      generateProgressBar(80),
                      "```",
                      "",
                      "---",
                      "",
                      "## 🔍 What happened",
                      "",
                      "Agent completed execution, but no Pull Request was found.",
                      "",
                      "**Possible causes:**",
                      "- Changes were not committed",
                      "- PR creation was skipped or failed",
                      "- Branch was not pushed to remote",
                      "",
                      "## 🔧 What to do next",
                      "",
                      "- Review the job logs to see what Claude did",
                      "- Check if there are local changes that need to be committed",
                      "- Manually create a PR or retry the agent",
                      "",
                      "---",
                      "*If you need help, review the execution logs for more details.*"
                    ].join("\n")
                  });
                } catch (e) {
                  console.log("无法更新进度评论:", e.message);
                }
              }

              // 移除 processing 标签，添加警告标签
              try {
                await github.rest.issues.removeLabel({
                  owner: context.repo.owner,
                  repo: context.repo.repo,
                  issue_number: issueNumber,
                  name: '🤖 ai-processing'
                });
              } catch (e) {}

              try {
                await github.rest.issues.removeLabel({
                  owner: context.repo.owner,
                  repo: context.repo.repo,
                  issue_number: issueNumber,
                  name: 'agent:simple'
                });
              } catch (e) {}

              await github.rest.issues.addLabels({
                owner: context.repo.owner,
                repo: context.repo.repo,
                issue_number: issueNumber,
                labels: ['⚠️ no-pr', 'needs-review']
              });

              // 不关闭 Issue，直接返回
              return;
            }

            // 检查 PR 的 CI 状态
            let checkRuns = [];
            try {
              const { data: checkData } = await github.rest.checks.listForRef({
                owner: context.repo.owner,
                repo: context.repo.repo,
                ref: relatedPR.head.sha,
                per_page: 50
              });
              checkRuns = checkData.check_runs || [];
            } catch (e) {
              console.log(`⚠️ 获取 Check Runs 失败: ${e.message}`);
            }

            const failedChecks = checkRuns.filter(run =>
              ['failure', 'cancelled', 'timed_out', 'action_required'].includes(run.conclusion)
            );
            const pendingChecks = checkRuns.filter(run => run.status !== 'completed');

            const summarizeChecks = (runs) =>
              runs.map(r => `- ${r.name}: ${r.conclusion || r.status}`).join('\n');

            // 进度条生成函数
            function generateProgressBar(percentage) {
              const totalBlocks = 20;
              const filledBlocks = Math.round((percentage / 100) * totalBlocks);
              const emptyBlocks = totalBlocks - filledBlocks;
              return '█'.repeat(filledBlocks) + '░'.repeat(emptyBlocks) + ` ${percentage}%`;
            }

            if (failedChecks.length > 0) {
              console.log(`❌ 检测到 ${failedChecks.length} 个失败的检查`);

              // 构建 claude-code-action 风格的 CI 失败状态
              const jobUrl = `https://github.com/${context.repo.owner}/${context.repo.repo}/actions/runs/${{ github.run_id }}`;
              const prUrl = relatedPR ? relatedPR.html_url : '';
              const statusParts = [`**🔨 CI checks failed** —— [View job](${jobUrl})`];
              if (prUrl) {
                statusParts.push(`[PR #${relatedPR.number}](${prUrl})`);
              }
              const statusLine = statusParts.join(' • ');

              const ciFailureSection = [
                statusLine,
                "",
                "```",
                generateProgressBar(75),
                "```",
                "",
                "---",
                "",
                "## ❌ Failed checks",
                "",
                summarizeChecks(failedChecks),
                "",
                "## 🔧 What to do next",
                "",
                "- Auto-fix has been triggered (if applicable)",
                "- Review the PR checks for detailed error messages",
                "- Manual fixes may be required for some issues",
                "",
                "---",
                "*The `/fix` command will attempt to resolve common CI failures automatically.*"
              ].join("\n");

              if (progressCommentId) {
                try {
                  await github.rest.issues.updateComment({
                    owner: context.repo.owner,
                    repo: context.repo.repo,
                    comment_id: progressCommentId,
                    body: [
                      "⚡ **Simple Agent**",
                      "",
                      "---",
                      "",
                      ciFailureSection
                    ].join("\n")
                  });
                } catch (e) {
                  console.log("无法更新进度评论:", e.message);
                }
              }

              const shouldTriggerFix = failedChecks.some(run => {
                const name = (run.name || '').toLowerCase();
                return name.includes('build') || name.includes('test') || name.includes('lint') || name.includes('type');
              });

              if (shouldTriggerFix) {
                await github.rest.issues.createComment({
                  owner: context.repo.owner,
                  repo: context.repo.repo,
                  issue_number: relatedPR.number,
                  body: [
                    "/fix",
                    "",
                    "---",
                    "🔄 **自动修复**：检测到 CI 失败，已触发修复流程。"
                  ].join("\n")
                });
              }

              try {
                await github.rest.issues.removeLabel({
                  owner: context.repo.owner,
                  repo: context.repo.repo,
                  issue_number: issueNumber,
                  name: '🤖 ai-processing'
                });
              } catch (e) {}

              try {
                await github.rest.issues.removeLabel({
                  owner: context.repo.owner,
                  repo: context.repo.repo,
                  issue_number: issueNumber,
                  name: 'agent:simple'
                });
              } catch (e) {}

              await github.rest.issues.addLabels({
                owner: context.repo.owner,
                repo: context.repo.repo,
                issue_number: issueNumber,
                labels: ['⚠️ ci-failed', 'needs-review']
              });

              return;
            }

            if (pendingChecks.length > 0) {
              console.log(`⏳ 仍有 ${pendingChecks.length} 个检查未完成`);

              // 进度条生成函数
              function generateProgressBar(percentage) {
                const totalBlocks = 20;
                const filledBlocks = Math.round((percentage / 100) * totalBlocks);
                const emptyBlocks = totalBlocks - filledBlocks;
                return '█'.repeat(filledBlocks) + '░'.repeat(emptyBlocks) + ` ${percentage}%`;
              }

              // 构建 claude-code-action 风格的 CI 进行中状态
              const jobUrl = `https://github.com/${context.repo.owner}/${context.repo.repo}/actions/runs/${{ github.run_id }}`;
              const prUrl = relatedPR ? relatedPR.html_url : '';
              const statusParts = [`**⏳ CI checks running** —— [View job](${jobUrl})`];
              if (prUrl) {
                statusParts.push(`[PR #${relatedPR.number}](${prUrl})`);
              }
              const statusLine = statusParts.join(' • ');

              const ciPendingSection = [
                statusLine,
                "",
                "```",
                generateProgressBar(75),
                "```",
                "",
                "---",
                "",
                "## ⏳ Pending checks",
                "",
                summarizeChecks(pendingChecks),
                "",
                "---",
                "*Waiting for all CI checks to complete...*"
              ].join("\n");

              if (progressCommentId) {
                try {
                  await github.rest.issues.updateComment({
                    owner: context.repo.owner,
                    repo: context.repo.repo,
                    comment_id: progressCommentId,
                    body: [
                      "⚡ **Simple Agent**",
                      "",
                      "---",
                      "",
                      ciPendingSection
                    ].join("\n")
                  });
                } catch (e) {
                  console.log("无法更新进度评论:", e.message);
                }
              }

              try {
                await github.rest.issues.removeLabel({
                  owner: context.repo.owner,
                  repo: context.repo.repo,
                  issue_number: issueNumber,
                  name: '🤖 ai-processing'
                });
              } catch (e) {}

              try {
                await github.rest.issues.removeLabel({
                  owner: context.repo.owner,
                  repo: context.repo.repo,
                  issue_number: issueNumber,
                  name: 'agent:simple'
                });
              } catch (e) {}

              await github.rest.issues.addLabels({
                owner: context.repo.owner,
                repo: context.repo.repo,
                issue_number: issueNumber,
                labels: ['⚠️ ci-pending', 'needs-review']
              });

              return;
            }

            // 进度条生成函数
            function generateProgressBar(percentage) {
              const totalBlocks = 20;
              const filledBlocks = Math.round((percentage / 100) * totalBlocks);
              const emptyBlocks = totalBlocks - filledBlocks;
              return '█'.repeat(filledBlocks) + '░'.repeat(emptyBlocks) + ` ${percentage}%`;
            }

            // 有 PR 的情况：正常完成流程
            // 更新进度评论为完成状态
            if (progressCommentId) {
              try {
                await github.rest.issues.updateComment({
                  owner: context.repo.owner,
                  repo: context.repo.repo,
                  comment_id: progressCommentId,
                  body: [
                    "⚡ **Simple Agent 启动**",
                    "",
                    "---",
                    "",
                    "## 📊 执行进度",
                    "",
                    "**当前阶段**: ✅ 完成",
                    "",
                    "```",
                    generateProgressBar(100),
                    "```",
                    "",
                    "**状态**: ✅ 任务已完成",
                    prInfo,
                    "",
                    "---",
                    "> 💡 简单任务将直接开始开发。"
                  ].join("\n")
                });
              } catch (e) {
                console.log("无法更新进度评论:", e.message);
              }
            }

            // 移除 processing 标签，添加 completed 标签
            try {
              await github.rest.issues.removeLabel({
                owner: context.repo.owner,
                repo: context.repo.repo,
                issue_number: issueNumber,
                name: '🤖 ai-processing'
              });
            } catch (e) {}

            try {
              await github.rest.issues.removeLabel({
                owner: context.repo.owner,
                repo: context.repo.repo,
                issue_number: issueNumber,
                name: 'agent:simple'
              });
            } catch (e) {}

            await github.rest.issues.addLabels({
              owner: context.repo.owner,
              repo: context.repo.repo,
              issue_number: issueNumber,
              labels: ['✅ ai-completed']
            });

            // 只有找到 PR 才关闭 Issue
            await github.rest.issues.update({
              owner: context.repo.owner,
              repo: context.repo.repo,
              issue_number: issueNumber,
              state: 'closed',
              state_reason: 'completed'
            });

            console.log(`✅ Issue #${issueNumber} 已自动关闭，将触发依赖链中的下一个任务`);

            // 发布成功评论
            await github.rest.issues.createComment({
              owner: context.repo.owner,
              repo: context.repo.repo,
              issue_number: issueNumber,
              body: [
                "✅ **Simple Agent 完成**",
                "",
                `PR 已创建: [#${relatedPR.number}](${relatedPR.html_url})`,
                "",
                "Issue 已自动关闭。",
                "",
                "---",
                "> 🤖 由 Vibe Simple Agent 自动处理"
              ].join("\n")
            });

      - name: Update Issue on Failure
        if: failure() && steps.check_duplicate.outputs.is_duplicate != 'true'
        uses: actions/github-script@v7
        with:
          script: |
            const issueNumber = ${{ steps.issue_info.outputs.issue_number }};
            const progressCommentId = '${{ steps.update_issue_status.outputs.progress_comment_id }}' || null;
            const errorMessage = `${{ steps.check_result.outputs.error_message || '未知错误' }}`;

            // 进度条生成函数
            function generateProgressBar(percentage) {
              const totalBlocks = 20;
              const filledBlocks = Math.round((percentage / 100) * totalBlocks);
              const emptyBlocks = totalBlocks - filledBlocks;
              return '█'.repeat(filledBlocks) + '░'.repeat(emptyBlocks) + ` ${percentage}%`;
            }

            // 分析错误类型并给出针对性建议
            let errorType = "";
            let errorIcon = "❌";
            let nextSteps = [];

            if (errorMessage.includes("Edit") || errorMessage.includes("old_string") || errorMessage.includes("not found")) {
              errorType = "Code edit failed";
              errorIcon = "🔍";
              nextSteps = [
                "The target code may have been modified",
                "Try reviewing the file manually or use `/agent-simple` to retry"
              ];
            } else if (errorMessage.includes("timeout") || errorMessage.includes("Timeout")) {
              errorType = "Task timeout";
              errorIcon = "⏱️";
              nextSteps = [
                "Task may be too complex for simple agent",
                "Use `/agent-medium` for more complex tasks"
              ];
            } else if (errorMessage.includes("rate limit") || errorMessage.includes("429") || errorMessage.includes("Too Many Requests")) {
              errorType = "API rate limit";
              errorIcon = "🚦";
              nextSteps = [
                "Wait a few minutes and retry with `/agent-simple`"
              ];
            } else if (errorMessage.includes("build") || errorMessage.includes("compile")) {
              errorType = "Build failed";
              errorIcon = "🔨";
              nextSteps = [
                "Code contains syntax or compilation errors",
                "Review the build logs for specific issues"
              ];
            } else {
              errorType = "Execution error";
              errorIcon = "⚠️";
              nextSteps = [
                "Review the Actions logs for more details",
                "Retry with `/agent-simple` or use `/agent-medium`"
              ];
            }

            // 构建 claude-code-action 风格的失败状态
            const jobUrl = `https://github.com/${{ github.repository }}/actions/runs/${{ github.run_id }}`;
            const statusLine = `**${errorIcon} Claude failed** — ${errorType} —— [View job](${jobUrl})`;

            // 更新进度评论为失败状态
            if (progressCommentId) {
              try {
                await github.rest.issues.updateComment({
                  owner: context.repo.owner,
                  repo: context.repo.repo,
                  comment_id: progressCommentId,
                  body: [
                    "⚡ **Simple Agent**",
                    "",
                    "---",
                    "",
                    statusLine,
                    "",
                    "```",
                    generateProgressBar(50),
                    "```",
                    "",
                    "---",
                    "",
                    "## 🔧 What to do next",
                    "",
                    ...nextSteps.map(step => `- ${step}`),
                    "",
                    "<details>",
                    "<summary>📋 Error details</summary>",
                    "",
                    "```",
                    errorMessage.substring(0, 500),
                    "```",
                    "</details>",
                    "",
                    "---",
                    "*For more complex tasks, consider using `/agent-medium` or `/agent-complex`.*"
                  ].join("\n")
                });
              } catch (e) {
                console.log("无法更新进度评论:", e.message);
              }
            }

            try {
              await github.rest.issues.removeLabel({
                owner: context.repo.owner,
                repo: context.repo.repo,
                issue_number: issueNumber,
                name: '🤖 ai-processing'
              });
            } catch (e) {}

            await github.rest.issues.addLabels({
              owner: context.repo.owner,
              repo: context.repo.repo,
              issue_number: issueNumber,
              labels: ['❌ ai-failed', 'needs-review']
            });

            // 不再创建额外评论，信息已在进度评论中
            console.log(`Issue #${issueNumber} 已标记为失败，错误: ${errorAnalysis}`);
