# 标签系统优化分析

## 📊 当前标签系统（16+ 个标签）

### 1️⃣ 复杂度标签（3个）
- `complexity:simple` - 简单任务
- `complexity:medium` - 中等任务
- `complexity:complex` - 复杂任务

### 2️⃣ Agent 类型标签（3个）⚠️ **冗余**
- `agent:simple` 
- `agent:medium`
- `agent:complex`（可能）

### 3️⃣ AI 状态标签（3个）⚠️ **使用 emoji**
- `🤖 ai-processing` - 处理中
- `✅ ai-completed` - 完成
- `❌ ai-failed` - 失败

### 4️⃣ 触发/范围标签（4个）⚠️ **职责不清**
- `needs-route` - 触发路由
- `backend` - 触发 + 范围
- `frontend` - 触发 + 范围
- `database` - 触发 + 范围

### 5️⃣ 跳过标签（4个）⚠️ **过多**
- `skip-vibe`
- `manual`
- `question`
- `discussion`

### 6️⃣ 临时状态标签（3个）⚠️ **过细**
- `⚠️ no-pr` - 没有创建 PR
- `⚠️ ci-failed` - CI 失败
- `⚠️ ci-pending` - CI 等待中

### 7️⃣ 其他标签
- `needs-review` - 需要审查
- `needs-triage` - 需要分类
- `ui-spec-ready` - UI Spec 准备好
- `feature:xxx` - 功能分支

---

## 🎯 优化方案（推荐）

### ✅ 核心原则
1. **移除冗余** - agent 标签和 complexity 标签信息重复
2. **清晰职责** - 分离触发和范围标签
3. **简化状态** - 减少临时状态标签
4. **改变触发方式** - 从自动触发改为手动触发

---

## 📋 优化后的标签系统（10 个核心标签）

### 1️⃣ 复杂度标签（3个）✅ 保留
```
complexity:simple
complexity:medium
complexity:complex
```

### 2️⃣ AI 状态标签（3个）🔄 去除 emoji
```
ai:processing    （原 🤖 ai-processing）
ai:completed     （原 ✅ ai-completed）
ai:failed        （原 ❌ ai-failed）
```

### 3️⃣ 范围标签（3个）✅ 保留
```
backend
frontend
database
```

### 4️⃣ 控制标签（1个）✅ 核心
```
needs-route      （手动触发路由的唯一方式）
```

### 5️⃣ 其他必要标签
```
needs-review     （合并所有临时状态，在评论中说明原因）
ui-spec-ready    （UI Spec 已生成）
feature:xxx      （功能分支，保留）
```

---

## 🔧 核心改变

### 改变 1：移除 agent:xxx 标签
**原因**：和 complexity:xxx 完全重复

```diff
- agent:simple
- agent:medium  
- agent:complex
+ 保留 complexity:simple/medium/complex
```

**影响**：几乎无，通过 complexity 标签就能知道用了哪个 Agent

### 改变 2：状态标签去除 emoji
**原因**：
- emoji 在某些系统显示不佳
- 不利于程序化处理（虽然现在可以用字符串匹配）
- 更专业和清晰

```diff
- 🤖 ai-processing
+ ai:processing

- ✅ ai-completed
+ ai:completed

- ❌ ai-failed
+ ai:failed
```

### 改变 3：改变触发方式（最大改变）🌟
**当前逻辑**：
```
创建 issue → 自动触发 Router（除非有跳过标签）
```

**优化后**：
```
创建 issue → 不自动触发
手动添加 needs-route → 触发 Router
```

**好处**：
- ✅ 不需要跳过标签了（skip-vibe, manual, question, discussion 全部移除）
- ✅ 开发者可以先整理需求，准备好了再触发
- ✅ 避免误触发
- ✅ 标签数量大幅减少

### 改变 4：合并临时状态标签
**原因**：`⚠️ no-pr`, `⚠️ ci-failed`, `⚠️ ci-pending` 都是临时状态

```diff
- ⚠️ no-pr
- ⚠️ ci-failed
- ⚠️ ci-pending
+ needs-review  （统一使用，在评论中说明具体原因）
```

### 改变 5：backend/frontend/database 仅作范围标签
**当前问题**：这些标签既是触发标签，又是范围标签，职责不清

**优化后**：
```
触发：只有 needs-route
范围：backend, frontend, database（由 Router 自动添加）
```

---

## 📊 对比表

| 类型 | 当前 | 优化后 | 减少 |
|------|------|--------|------|
| 复杂度标签 | 3 | 3 | 0 |
| Agent 标签 | 3 | **0** | -3 ✅ |
| 状态标签 | 3 | 3 | 0 |
| 触发标签 | 4 | 1 | -3 ✅ |
| 跳过标签 | 4 | **0** | -4 ✅ |
| 临时状态 | 3 | **1** | -2 ✅ |
| 其他 | 3 | 2 | -1 |
| **总计** | **23** | **10** | **-13** ✅ |

---

## 🚀 实施步骤

### 步骤 1：修改 vibe-router.yml
```yaml
on:
  issues:
    types: [labeled]  # 移除 opened，只在打标签时触发
```

### 步骤 2：修改标签检查逻辑
```javascript
// 只有 needs-route 触发
const triggerLabels = ['needs-route'];  // 移除 backend, frontend, database

// 移除跳过标签检查（不需要了）
// 移除标题模式检查（不需要了）
```

### 步骤 3：批量替换状态标签
```bash
# 在所有 workflow 中替换
🤖 ai-processing → ai:processing
✅ ai-completed → ai:completed  
❌ ai-failed → ai:failed
```

### 步骤 4：移除 agent:xxx 标签
```bash
# 在 agent-simple.yml, agent-medium.yml, agent-complex.yml 中
# 移除所有 agent:simple, agent:medium, agent:complex 的添加/移除代码
```

### 步骤 5：合并临时状态标签
```bash
# 统一使用 needs-review，在评论中说明原因
⚠️ no-pr → needs-review + 评论说明
⚠️ ci-failed → needs-review + 评论说明  
⚠️ ci-pending → needs-review + 评论说明
```

---

## 🎯 优化后的工作流

### 创建新需求
```
1. 创建 issue（不会触发任何自动化）
2. 慢慢完善需求
3. 准备好后，添加 needs-route 标签
4. Router 自动分析复杂度并路由
```

### 标签状态流转
```
needs-route 
    ↓
complexity:medium + backend + ai:processing
    ↓
ai:completed  或  ai:failed + needs-review
```

---

## ❓ 权衡考虑

### 优点
- ✅ 标签数量从 23 个减少到 10 个（减少 57%）
- ✅ 逻辑更清晰，职责更明确
- ✅ 避免误触发和重复触发
- ✅ 开发者体验更好（可以先整理需求）
- ✅ 不需要记忆多个跳过标签

### 缺点
- ⚠️ 需要手动添加 needs-route 标签（但这正是优点）
- ⚠️ 需要修改多个 workflow 文件（一次性工作）
- ⚠️ 旧的 issue 可能还有旧标签（可以逐步清理）

---

## 💡 建议

**推荐采用此优化方案**，因为：

1. **解决了你的痛点**：不需要 skip-vibe 标签，默认就不会触发
2. **大幅简化**：标签数量减少 57%
3. **逻辑更清晰**：触发、范围、状态职责分明
4. **向后兼容**：可以逐步迁移，不影响现有功能

---

## 🔄 渐进式迁移策略

如果担心一次性改动太大，可以分阶段实施：

### 阶段 1（影响最小）
- 移除 agent:xxx 标签
- 去除状态标签的 emoji

### 阶段 2（核心优化）  
- 改变触发方式（只保留 needs-route）
- 移除跳过标签

### 阶段 3（细节优化）
- 合并临时状态标签

---

**是否需要我帮你实施这个优化？**
